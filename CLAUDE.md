# PunctuatorsGame — Repo Guide

This repo is a **single flat-hosted static website** (GitHub Pages): an `index.html` hub plus many
interlinked word-game and word-tool pages, shared CSS/JS (`nav.js`, `theme.css`, …), and shared data/art
assets. Several self-contained HTML games live here alongside the site's other tools.

## Read the right doc first

Per-game details live in `docs/` so this root file stays small and every session loads only what's
relevant. **Before working on a game, read its doc.** Keep that doc current in the *same* change whenever
you add/remove/modify a feature (treat the doc as part of the diff, not an afterthought).

| Game | Entry file | Doc |
| ---- | ---------- | --- |
| **Inklings** — word-collecting adventure (hunt letter-creatures, spell words at a desk, fill a library) | `inklings.html` | [`docs/inklings.md`](docs/inklings.md) · [architecture/maps/portability](docs/inklings-architecture.md) · [grammar systems/edu design](docs/inklings-grammar-systems.md) · [farming/scripts](docs/inklings-farming.md) · [collections/curation/decoration](docs/inklings-collections.md) · [object placement/cozy square (0,1)](docs/inklings-placement.md) · [poetry/phoneme engine](docs/inklings-poetry.md) · [phoneme fishing (tentative)](docs/inklings-fishing.md) · [heraldry/blazon shield](docs/inklings-heraldry.md) · [story companions](docs/inklings-companions.md) · [signal flags/omen mast (tentative)](docs/inklings-signal-flags.md) · [cipher menagerie (tentative)](docs/inklings-ciphers.md) · [sound-effect minibosses/shouts (tentative)](docs/inklings-shouts.md) · [diacritics/diacryptids (tentative)](docs/inklings-diacritics.md) |
| **Kaimoju** — Rampage-style kaiju that destroys buildings by typing romaji for kana/kanji | `kaimoju.html` | [`docs/kaimoju.md`](docs/kaimoju.md) |
| **Specimen Lab** (Exquisite Corpse) — Rummikub-style monster-parts card game on canvas | `exquisite-corpse-rummy.html` | [`docs/exquisite-corpse.md`](docs/exquisite-corpse.md) |
| **Spin Nids** (Ambigram Word Game) — rotatable ambigram-tile word game | `SpinNidIndex.html` | [`docs/spin-nids.md`](docs/spin-nids.md) |
| **Write. Right!** — sentence-building card game with an exquisite-corpse drawing twist | `WriteRight.html` | [`docs/write-right.md`](docs/write-right.md) |
| **The Prescriptivist's Gauntlet** — transcribe sentences under stacking linguistic "laws" | `Prescriptivist.html` | [`docs/prescriptivist.md`](docs/prescriptivist.md) |
| **Pitch Bird** — sing-to-fly voice game; pitch controls the bird's height (Flappy pipes + UltraStar karaoke). *In development, not yet shipped* | `pitch-bird.html` | [`docs/pitch-bird.md`](docs/pitch-bird.md) |
| **Mujicians** — Balatro-style music-theory deckbuilder: cards are notes (ROYGBIV), "poker hands" are chords/scales, score = theory correctness, and every hand is played as audio. *v1 vertical slice built (deck→hand→score→sound; **gigs removed 2026-07-17** — a run is now **one continuous Mario-Paint-style performance in one fixed key** (C major, one Muse drafted at the start) filling a single loop; daily cap; **Save-a-Song** = name/keep the **whole run's** song with a theory report card, replay/export from a Home Setlist); economy still to come. **Threshold removed 2026-07-18 → open-ended performance** (no score gate ends a run; player presses **✓ Finish song** when done, or the loop auto-completes when stage space runs out, warned at ≤3 bars left; win/lose collapsed to one "Performance complete"; `winRun`/`loseRun`→`finishRun`; campaign movements still advance by their **skill-demo gate**; `runThreshold`/`RUN_THRESHOLD` kept vestigial for a future non-blocking "applause star"). **AMENDED 2026-07-19 — open-ended is now FREE-PLAY-ONLY; every Campaign run has a per-run GOAL that stops the song** (that movement's gate, tracked this run — M1 = play all 7 letters A–G via `run.gatePitch`, M2 = each note value, …): meeting it pops a **choice — 🏁 Finish & score / ✏️ Keep building** (`showGoalPrompt`/`runGoalMet`); the **✓ Finish button is removed from Campaign** (returns only after *Keep building*; Free Play keeps it + stays open-ended); M1 shows a **pitch-accuracy score** (`pitchAccuracy()` = avg per-note nearness → %+grade); advancement now needs the goal met **in one run** (M1 letters no longer accumulate across runs — `persist…gates.pitch` legacy). **Call-and-response scoring** (designed 2026-07-18): the element-spirit plays a **call**, you **respond**, scored by **graded proximity** — fixes "starting-level card choice doesn't matter" (all 7 diatonic cards scored the same); it's the **Campaign** scoring spine while **Free Play stays open-ended**; shown-early→ear-only ramp; **Dynamics** teaches loudness notation via a **sleeping-creature** scene (sneak quiet+rests vs scare loud); **later movements turn from echo → freeform composition** scored on the green `fitsSelection()` "sounds-good" cells so users make original soundtracks; **saved song = your notes only** (call is an off-timeline cue — decided 2026-07-19, no dialogue). **M1 Pitch BUILT (2026-07-19):** `run.call` = a random in-key note (sounded; 🔊 replay), a `respond` score term (**raw pitch nearness — reworked 2026-07-19**: absolute `|played−call|` semitone distance, per-semitone ramp that never hits zero, NOT consonance — that's deferred to Melody), a "🎧 Match this note" call bar, M1-campaign-only via `callActive()`; same pass fixed a **backing-loop scheduler look-ahead race** (a note placed in the ~120ms committed window skipped its loop echo for one lap — `catchUpEvent` in `placeEvent`). **Amped reward feedback (2026-07-19):** the **4 nearness tiers** (`respondTier(midi)` → exact/soclose/close/far, returned as `res.respond`) now drive loud Balatro-style juice — a **tier-graded cell bloom** (gold burst / amber / green / faint gray), a **floating rating word** (✦ PERFECT! / 🔥 SO CLOSE! / 👍 Close / · far ·), a **soft inharmonic reward bell** (`_bell()`/`respondChime`; warmth tracks proximity — reworked 2026-07-23 from a bare triangle tone that sounded like a played note, to a struck-metal chime distinct from the melody), and a **live pre-commit** call-bar glow + verdict stamp + preview tier tag; scoring balance unchanged (presentation only). **Blind "by Ear" M1 sub-level BUILT (2026-07-19):** the shown→ear-only ramp is a **separate Home sub-level** (unlocked once the 7 shown naturals are catalogued) — `startRun(mode,opts)` gained `opts.movement`/`opts.pitchLevel`; `run.pitchLevel:"ear"` + `callHidden()` make the call **sound-only** (muffled `?` chip, **no pre-commit verdict**, blank numeric preview so you can't brute-force by watching the applause), you commit **blind**, the bloom/rating-word/chime judge you *after*, and a **reveal banner** (`run.callReveal`, its own prominent tier-coloured row) then names the note ("The note was E ✓ — you played D") and **stays until you select your next answer card** (`toggleSel` clears it; no short timer to miss); goal unchanged (catalogue 7 letters, now by ear), reports pitch accuracy "(by ear)", sets `persist…gates.pitchEar` for a Home "✓"; forced `movement:1` so it can't double-advance past M1; Free Play/M2–M7 untouched. **Mentor lesson intros BUILT (2026-07-19):** a Balatro-joker-style **character card + speech bubble** explains each campaign level's music theory + task at run start, **before the Muse draft** (`startRun`→`offerIntro`→`screen:"intro"`→draft→play; Free Play skips it); **full script first time → compact one-liner + "more ▾" on repeat** (`persist.progress.seenIntros`); two swap-friendly registries — `CHARACTERS` (art is a **token**: `🍄` emoji now → pixel sprite via `charArtHTML()`, one place) + `LESSON_INTROS` (keyed `m1`/`m1:ear`/`m2…m7`, per-entry `char` = who speaks — the seam for a per-level mentor); **real authored dialogue for all M1–M7** (each tied to its story element — rhythm=earth, dynamics=fire, melody=water, harmony=metal, timbre=wood, structure=time — and accurate to that level's mechanic + gate). **Per-movement mentors wired (Stage A, 2026-07-25):** the full *Mujicians* graphic-novel cast now populates `CHARACTERS` (12 heroes + 7 villains, emoji art tokens, sprite-ready) and **each movement's hero narrates its own lesson in-voice**, foiling its villain in the dialogue — Dee M1 naturals, Sharpist/Sir Flatterer M1 ♯/♭, **Gaia** (City Gnome) M2, **Crescendra** (Frankenstein woman) M3, **Cantrip** (goblin) M4, **Mage Orc** M5 (with band Miner Minor/Demonish/Augminotaur), **Timbrewolf** M6, **The Key** (rogue) M7; villains **Ranger/Sandmar/Morendo/Slurry/Tritony/Wormwood(tentative)/The Bartender** are seated as **dialogue-first foils now, boss mechanics deferred** (Ranger's ♮ boss + two fleshed-out designs are designed-not-built: **Sandmar's M2 "Lullaby Duel"** (the **boss template** — a call-and-response drum duel on the Beat Lab engine, tug-of-war Asleep⟷Awake meter, telegraphed rest-hex / gravity-drag / yawn-blackout debuffs, `run.debuff='drowsy'`, capture → "Gravity Groove" Muse) and **Morendo's M3 "Scorch the Bones"** (off-grid **first-person loudness/mic set-piece** reusing Pitch Bird's mic: whisper=*piano* to lure the skeleton close, erupt=*forte* into a bottom-of-screen peak-meter fire to scorch him, Breath-recharge mana, voice-steal→hidden-size p/mf/f card quiz, `run.debuff='draining'`, capture → "Dying Fall" Muse) — **standalone core-loop MVP BUILT 2026-07-25 in `scorch-bones.html`** (mic RMS loudness + sensitivity slider, three zones, peak-meter fire, distance-scaling sprite, wander/whisper-aggro/approach AI, erupt-scorch + Breath gate + Torch life, win/lose + retry, no-mic Space-pump fallback; isolated to feel-test before porting in — next: card quiz, phases, capture, canvas look). **Slurry's M4 "Stepping Stones"** designed-not-built (the **pitch** set-piece, reusing Pitch Bird's pitch detection: side-scroll stream, voice-pitch=height, sing a stone's pitch to hop a stepwise melody across, blur-the-note→sing by ear, flood fail-state, cross-and-chip = her HP, `run.debuff='smear'`, capture → tamed-portamento Muse; **extra element beyond the platformer (2026-07-26): Cantrip's garments = scales/modes** — his graphic-novel power (gain/impart a skill via worn clothing) becomes a swap-a-scale-to-bridge-the-gap puzzle where a garment=a mode reshapes which stepping-stones exist (real modes taught by their characteristic note: pentatonic/Ionian/Aeolian/Dorian/Lydian/Mixolydian/Phrygian/Locrian), a Wardrobe collection feeds Free Play, Slurry's soak/smear strip your garment; platformer = MVP, garment layer = 2nd pass). **Tritony's M5 "The Devil's Forge"** — **standalone MVP BUILT 2026-07-26 in `forge-quench.html`, then reworked same day into the real Opus-Magnum "program-the-machine" model** (lay an instruction **tape** from a glyph palette → **Draft** to preview free → **Forge** once, an arm walks the tape; glyphs +M3/+m3/Temper/Grind-invert/Etch-7th/Anneal-resolve, each a theory concept; build-to-spec orders quality×root×metal + inversion/bass spec + ghost target; **OM optimization scoring Cost·Cycles·Parts + Single-Heat bonus → damage**; flaw-injection tritone debuff Anneal'd out; tagged-tone `{m,flaw}` stack; 3 HP-gated phases → dom7 finisher; Torch lives; win/lose-retry; reuses classify + VOICES/playVoice. **Recycling/salvage layer BUILT same day** (theme: the Harmony band was a Recycled Orchestra of Cateura — "nothing is junk, even the tritone can sing" = resolve-dissonance ≈ redeem-discard, the connective tissue between Tritony & the 4 heroes): forge from salvaged junk = real recycled instruments (oil drum→steelpan, cans→guitar, forks→mbira, pipe→pan-pipe, bottles→xylophone, each a VOICES timbre), scrap spawns with rust/dent defects (brittle 2nd fail-source) fixed by new 🧽 De-rust/🔨 Straighten glyphs, ♻ Redemption bonus for reviving worse scrap. Isolated to feel-test before porting — deferred: spatial arms/tracks/pistons contraption, wider glyph catalog Draw/Fold/Punch/Pin/Project+reagents, cadence V7→I orders, Re-melt+more junk/defects, persistent cross-run scrap heap, alloy-ratio puzzle, art, campaign wiring) — the **harmony** set-piece (deliberately NOT a 4th mic game: an off-grid **Opus-Magnum-style chord-forging craft puzzle** on the *vertical* axis: an **alloy = chord** metaphor, stack thirds onto a given root ingot to forge a target-quality weapon-instrument, ▶ Run → strike-test = the chord rings clean/holds or cracks-if-dissonant; MVP = linear stack-builder but with real puzzle difficulty via composable primitives + economy/optimization scoring + heat budget + avoid-the-flaw; `run.debuff='dissonant'` = she injects a tritone-flaw you must Temper out; build-to-spec orders first, resolve-the-tritone tentative; **real-metallurgy accuracy layer** — the ingot is an actual instrument metal/alloy (brass/bronze/bell-bronze/nickel-silver/silver/gold/aluminum/steel/phosphor-bronze) whose real recipe sets the weapon's **timbre** via `VOICES`/`playVoice` (M5→M6 bridge), with a deferred alloy-ratio mixing puzzle (wrong ratio = brittle, e.g. bell bronze ~20% tin near-brittle by design); capture → "The Devil's Bargain" tamed-tritone Muse). **Wormwood's M6 "Wormwood's Choir"** designed-not-built (the **timbre** set-piece — the first **pure-listening, point-don't-perform** boss, no mic: he's a wooden-toothed busker who bites people so they all sound like him — a homogenized **choir** of hooded figures droning one flat timbre; you **scan/solo and pluck the odd true voice** — a real `VOICES` colour hidden among the flatten — to **restore** it; find-the-odd-voice MVP + deferred match-the-twin phase, `run.debuff='flatten'` (bite homogenizes/re-bites), HP=voices restored, it's the **Sound Collective exam**; hero Timbrewolf = his accidental masterpiece, ear-guide; capture → "Choirmaster" recolor-a-note + timbre-variety Muse; a **Murdle-style "Choir Line-Up" deduction variant is documented as an alternative framing** (2026-07-27, not locked) — choristers × timbre attributes as a whodunit grid where clues are ear-verifiable, an alt/harder mode over the find-the-odd-voice MVP, not a replacement). **The Bartender's M7 "Last Call"** designed-not-built (the **structure** capstone/final boss — the only *across-time* modality (form/memory), rendered as a **real musical staff** (not the grid) so bars + roadmap symbols read natively: "bars" pun/last-call bartender who **muddles your memory**; the fight's shape *is* A·B·A — **state an A theme (segno 𝄋), the night blurs (B + false-memory decoy), then write *D.S. al Coda* to recall A home from memory** over the built `pcSetFp`/`hasABA`; teaches segno/coda/D.S. as the notation of "remember & reuse"; hero **The Key** = the return/roadmap specialist (key-signature theory stays in Pitch/Melody; his name puns on the lock-key home); `run.debuff='muddle'`, HP=song-gets-home; capture → "The Regular"/Reprise theme-and-variations + callback Muse). **Ranger's M1 ♮ "Natural Selection"** now fleshed-out (designed-not-built; the *encounter* rounds out the mechanics spec already in the Accidentals section) — the **one boss fought on the core card gig** = Mujicians' first **Balatro-style Boss Blind** (constraint, not a set-piece), fitting a first boss: Ranger (wilderness archer, ♮=*nature*) looses **♮ arrows that naturalize your ♯/♭ back to their natural pitch** (`run.debuff='naturalize'`), you **win by making theory-correct music that still hits the target** (`runThreshold` — the one everyday run with a win check), MVP=full naturalization + telegraphed-arrow/volley phases deferred; capture → the **♮ (natural) card** (cancels a ♯/♭, seeds the Accidental-Tarot shop) + Ranger Muse. **All 7 bosses now designed (none built). Bosses by modality: Ranger=accidentals (♮/voice-leading, on-gig Boss Blind) · Sandmar=timing · Morendo=loudness · Slurry=pitch · Tritony=harmony (chord-forging) · Wormwood=timbre (listening) · The Bartender=structure (form/memory).** Deferred: Jiminy-style in-play follower (bubble on the mentor, not a modal), pixel art, villains' boss fights, cast-as-Muses (Stage D). See [`docs/mujicians.md`](docs/mujicians.md) "The full cast" + the "Sandmar"/"Morendo" boss sections. **Accidentals + M1 gated ladder BUILT (2026-07-19):** M1 is now a **gated ladder** `persist.progress.pitchStage` = **naturals → by-Ear → sharps → flats**, walked in order — **M2 Rhythm only unlocks past Flats** (by-Ear moved *into* the ladder right after the first pitch lesson, per dev; the standalone 🎧 Home button is gone → Campaign button follows the stage + a `pitchLadderHTML` strip; a **🎼 Pitch practice** replay row appears post-M1). **Sharps/Flats** = call-and-response: cards carry an **`acc`** field (`SHARP_NOTES`/`FLAT_NOTES`, same pcs, different `letter`+`acc` — the enharmonic fix), `shadeFor`/`noteLabel`/`rowLetter` render the spelling + a colour tinted toward the resolution; the mentor **sounds a natural** and you answer with **any note higher (♯) / lower (♭)** — **forgiving DIRECTION play, reworked 2026-07-20** (random draws made "play the *exact* accidental" frustrating): the exact ♯/♭ is a **bonus** and only an exact hit opens the two-play **resolution** (♯→up · ♭→down, also a bonus). Scoring (`score()` accidental branch): exact `+5 mult/+8 chips`, correct direction `+3 mult/+2 chips`, wrong way `+1 mult` (never zero); `respondTier` is direction-aware; `newCall` **never calls a note with no room in the required direction** (filters by deck range, so the top note for ♯ / bottom for ♭ is unanswerable-safe). **Gate** = **`ACC_DIR_TARGET`(4) correct-direction responses (`run.gateDir`) + one exact accidental (`run.gateAcc.size≥1`)**; resolution (`run.gateResolved`) is optional, shown via `accTrackerHTML`; mentors **🎭 Sharpist** (jester, giddy/up) + **🤺 Sir Flatterer** (knight, glum/down) narrate `LESSON_INTROS["m1:sharps"/"m1:flats"]`. **♮ boss deferred.** **Accidentals graduate to all later modes + Free Play BUILT (2026-07-20):** once BOTH M1 lessons clear (`accidentalsUnlocked()` = `persist.progress.movement>1`), `buildDeck`'s `else if(m>=2 && accidentalsUnlocked())` branch adds **BOTH spellings of every black key** (C♯ *and* D♭ — the enharmonic pair), **`ACC_COPIES`(1)/timbre**, deck **capped at `MAX_TIMBRES`(4)** (`instrumentsFor().slice(0,4)`); Free-Play gating keys off **campaign progress** so a first-timer meets them only after being taught. The **chromatic-resolution bonus** now fires in **every** mode (guard `isAccidentalLevel() || accidentalsUnlocked()`; `addCodex` in any mode) so accidentals are worth playing as leading/passing tones (still no `inkey ×2` — resolving is the payoff). Piano-roll honours spelling: `eventCoverage` carries a per-cell colour (`shadeFor(nt)` — C♯ warm vs D♭ cool), the ghost uses `selCol`, and black-key **row labels show both spellings** (`enharmonicPair` "C♯/D♭"). **Future: a dedicated enharmonics lesson** (why spelling depends on key/direction) — flagged, not built. **M2 Rhythm reworked into a LIVE finger-drumming rhythm game (designed 2026-07-20):** home-row keys (`jkl`/`asd`) fire refilling drum "Beatling" slots + `space`=bass, two live rounds (melody then beat over it), rests are free (silence = no key), creatures = **Beatlings** (Boombardier/Snarab/Ticker), graduates like accidentals via `rhythmUnlocked()`. **Stage-1 MVP BUILT 2026-07-21** = a standalone **🥁 Beat Lab** practice screen (Home button, no daily cap; new `_drum` kick/snare/hat noise voice, keyboard+touch polyphonic, timing Perfect/Good/Miss + combo + accuracy %, loop-pedal). **Beat-loop fixes BUILT 2026-07-21:** recorded beat is a **bounded, deduped set** (`labLoop`; fixed the stop/overload) with a **tunable cap** (`LAB_FILL_PCT` 0.35 → FIFO-evict oldest, dedup per slot); **backing riff fades** over `LAB_LEAD_LAPS`(4) as a tempo scaffold + a **🎵 re-summon** button; scheduler **fell-behind snap** for backgrounded tabs. **Stage 2 S2.1 BUILT 2026-07-21 — drums are first-class timeline events:** a `{drum:<voice>, at, dyn}` event (pitchless one-shot; `DRUM_VOICES=["kick","snare","hat"]` = canonical save index) that `scheduleEvent`/`snapshotEvents` handle + **MJ2 share-code kind `2`** `[2,at,voiceIdx]` (additive, old codes still decode); Beat Lab **💾 Keep this beat** saves the backing riff + your beat as one Setlist song. **S2.2 BUILT 2026-07-22 — gig keyboard shortcuts** (NOT "melody goes live" — dev reframed; melody stays own-pace, a live-melody mode is a deferred future option): select a hand card with a key + **Space = Play Hand**; `persist.hand` (right/left, shared w/ Beat Lab) picks the side, home row extends outward as the hand grows to 5–6 (right `h j k l ; '` / left `⇪ a s d f g`, `handKeysFor`); `MAX_HAND_SIZE`=6 cap (Muse rework deferred); no on-card badge (kept clear for future art); 🤚/✋ toggle in gig controls. **S2.3 Increment 1 BUILT 2026-07-22 — campaign M2 integration (foundation + Stage 1 "Match"):** the Beat Lab is now a configurable live-drum engine (`labCfg {mode,stage,target}`) shared by free **practice** + **campaign**; an M2 ladder `persist.progress.rhythmStage` **match→grooves→free** (mirrors M1's pitchStage; `RHYTHM_STAGES`, `rhythmUnlocked()`=movement>2, migration, Home `rhythmLadderHTML` strip); a campaign M2 run gets Dee's rewritten live-drumming intro (`m2`/`m2:grooves`/`m2:free`) then **skips the Muse draft** (`enterRhythmStage`) into the drum session; **Stage 1 "Match"** = four-on-the-floor (`GROOVES` `{id,name,hits}`) drawn as a ghost overlay, each Good+ hit landing a target cell (`grooveKey` = voice+8th-col) collects it, gate = collect all cells (`run.grooveMatched`/`grooveMet`, `.labgate` HUD), pass → `rhythmwin` screen + `maybeAdvance` walks the ladder / unlocks M3. **Free-stage melody round BUILT 2026-07-23** ("play your own beat over your melody; it becomes the song's backing loop"): the free stage no longer skips the draft (`dismissIntro` routes only match/grooves to `enterRhythmStage`) — **Phase 1** = compose a melody in the normal gig (own pace), the Campaign ✓ Finish button replaced by **🥁 Lay a beat over it** (enabled at ≥`FREE_MELODY_NOTES`(6) notes; a full stage ends composing, not the run); **Phase 2** (`enterFreeBeat`) = the composed melody becomes the drum session's backing loop via `labSetBacking` (`labBacking`/`labBackingTicks`/`labBackingFades` — plays at full volume, looped at its own whole-bar length, decoupled from the 2-bar drum loop by `labSchedBackingSeg`; `LAB_MELODY` scaffold still fades for practice/match/grooves), you finger-drum over it, and passing at `FREE_TAKE_HITS`(17) **total hits played** (`labStats.total`, HUD live per hit) **bakes** the recorded beat across the song as `{drum}` events (`bakeFreeBeat`) so `run.loop.events` = melody + beat, one keepable track (💾 Save on the `rhythmwin` screen). **Unified grid + drum graduation + count-in BUILT 2026-07-23:** the gig display went to **8th-note columns** (`DISPLAY_COLS_PER_BEAT=2`) so the note piano-roll and **kick/snare/hat drum lanes now share one grid + one playhead** (`loopStripHTML` draws the lanes from `drumCov`, shown when `rhythmUnlocked()` or the song has drums; existing `paintPlayCol` sweeps both — no new scheduler); drums **graduate to M3–M7 + Free Play** via a **🥁 Lay a beat over it** button next to Play/Discard (`graduatedBeatContext()`; Free Play or campaign mv≥3, always shown, disabled with an (n/6) counter until ≥`FREE_MELODY_NOTES` notes) → `layBeatOverSong()` reuses the free-take drum engine but sets **`run.beatReturn`**; the graduated pass is a **loop-pedal that keeps running until the player presses ✓ Done** (`finishGraduatedBeat`; `labCampaignScore` auto-finish guarded by `!run.beatReturn`), which routes through `rhythmStagePass` to **bake the beat and return to the gig** (not `rhythmwin`) with melody+drums on the unified grid; a **4-beat count-in** (`countInThen` — big 1·2·3·4 + hat clicks at tempo) precedes **every** live drum pass (practice/M2/graduated), not tempo-restarts. **Still simplified/next:** grooves stage matches one target (`backbeat`), not the shown→memory backbeat→son-clave→shuffle walk; Free-Play M2 keeps the legacy `gateDurs` gate. **Dev asks deferred (2026-07-24):** run the live drum pass **on the small unified gig grid** (not the separate larger-celled Beat Lab — "fine for now"), and draw the **Beatling pads as note cards** (not wide buttons). **M2 note-value & rest *notation* via the Break mechanic — Increments 1 & 2 BUILT 2026-07-24 (gesture juice = Increment 3 still designed-not-built):** two self-paced *notation* stages prepend the M2 ladder → **durations → rests → match → grooves → free** (notation stages run on the normal gig, timing stages on the drum engine); the verb is **Break** (break a card into two half-value cards; **Merge** = inverse, teaches ties) — so the lesson **starts at the quarter note** (both directions from turn one), same verb serves notes *and* rests (durations first, rests second). **Increment 1:** `DURATIONS` gained an **eighth** (`"e"`, 12t, the Break floor) + `DUR_LADDER`/`halfDur`/`doubleDur`; every card carries **`c.dur`** (default ♩); **`run.curDur` retired** → `selDurId()`/`selDurTicks()` read the selection (a note stack shares the **longest** selected value); **`breakSelected()`/`mergeSelected()`** (single selected card; Merge fuses the **nearest equal** mergeable partner anywhere in hand — relaxed from adjacent) wired to a **`breakControlHTML`** row (keys **`x`/`m`**) replacing `durControlHTML`; card shows its value via a **corner `noteheadSVG`** + SVG rest glyphs (`restSVG` — both draw as inline SVG, not the astral-plane Unicode note/rest glyphs which tofu; a fixed **`GLYPH_INK`** colour, NOT the note colour — yellow C was illegible) + **bottom length-bar** (`lenBarHTML`), gated to `termOn("groove")` (M2+/Free Play; M1 stays plain). Chords (M4+) disable Break/Merge. **Increment 2:** `RHYTHM_STAGES=["durations","rests","match","grooves","free"]` (default/DEV-reset `durations`; one-time `rhythmNotationV1` migration); notation stages compose on the normal gig (`dismissIntro` sends only match/grooves to the drum engine) via a **duration-focused deck** (`buildNotationDeck` — one tonic pitch + rests so every value incl. the whole is reachable), use the standard campaign goal→finish→`maybeAdvance` ladder walk; **gates** = durations plays each of ♪♩𝅗𝅥𝅝 (`gateDurs`), rests places each of 𝄾𝄽𝄼𝄻 (new `gateRestDurs`); Dee intros `m2:durations`/`m2:rests` + a bridged `m2` intro; `RHYTHM_STAGE_LABEL`/Home ladder/win-screen advancement made movement-aware. Simplified for MVP: rests gate is "each rest value" (not "mix notes+rests"), durations is a value-checklist (not a target rhythm to match). **Increment 3 BUILT 2026-07-24 (gestures + dynamics gesture + length-bar fade + glyph cleanup):** card **vertical swipe** = Break↓/Merge↑ (`breakAt`/`mergeAt`; pointer events + `setPointerCapture`, `.card` `touch-action:none`; thresholds `CARD_SWIPE_MIN`24/`CARD_TAP_MAX`14), **tap**=select, **double-tap**=louder (`onCardTap`→`loudenCard`, M3+; the swipe/dynamics axis conflict resolved to **double-tap, not drag**, since swipe is taken by Break/Merge); **length-bar** faded (.28) unless `notationLesson()`; **glyph cleanup** = `noteGlyph`/`restGlyph` inline SVG in M2 gate labels + `m2:durations`/`m2:rests` intros (hoisted fn decls — `LESSON_INTROS` evaluates at load; `DURATIONS.label`/`.rest` now dead-for-display). **Crack-and-fall-apart animation BUILT 2026-07-24:** Break/Merge animate on `#fxlayer` (overlay clones + FLIP, reduced-motion cuts to instant) — Break (`flyBreak`) = a `--cc`-tinted crack flash down the centre + two clip-path half-clones (`.fxhalf`) that fall to the new slots as the real half-value cards pop in (`.card.popin`); Merge (`flyMerge`) = two clones slide together into the merged card's slot; `breakAt`/`mergeAt` capture source rects/colour pre-render so button/key/swipe all get it; a `_crack(merge)` SFX (filtered-noise snap ↓ / fuse ↑) plays on both, even under reduced motion. **Break is a first-class control in Free Play too, not lessons-only; duration becomes a per-card property (every card defaults to ♩, reshaped by Break/Merge). DECIDED 2026-07-24: `run.curDur` (the per-play duration picker) retires in favour of per-card Break everywhere** — still the live shipped control until Break is built; migrating the `curDur` callsites is part of building Break. Card shows its value playing-card-style (71×95, corner index = letter + **SVG** notehead — not the astral-plane Unicode half/whole glyphs — plus a fadeable bottom length-bar). Input is decoupled (button + key MVP, downward "snap" swipe as Layer-2 juice; single-pointer only, no hold-for-Break — hold reserved for deferred hold-to-sustain); Dynamics gains a card gesture to A/B (double-tap = louder vs. vertical drag; pinch not load-bearing); tentative future: **merge unequal same-pitch cards → dotted/tied values** (sum ticks, e.g. ♩+♪=dotted quarter — the *preferred* dotted-note route, easier than the uneven-Break swipe; needs `c.dur` to hold raw ticks or a dot flag), uneven Break→dotted (alt), a **Prism** tool-card (ROYGBIV refraction; optional flavour/economy, never a gate on free Break), a "Break-ling" creature — see [`docs/mujicians.md`](docs/mujicians.md) "the Break mechanic". **Other M3–M7 call-and-response + the Dynamics/freeform sub-designs remain to build.** **Still planned, not built:** a persistent Home **"backstage" shop paid in "Tips"** (a separate currency from applause — applause stays a pure score), selling Muses/notes/Étude/Accidental cards/+loop-bars into a Free-Play loadout; persisting gate counters across runs; **mobile landscape support** (responsive CSS + "rotate your phone" nudge, since the grid is DOM not canvas; Android fullscreen orientation-lock as progressive enhancement — iOS Safari can't force orientation; pairs with raising `LOOP_BARS` for longer songs); and **timbre as collectible translucent card skins** (Balatro-edition-style overlays that each carry a hand-rolled Web-Audio synth **preset** + a pure-CSS sheen — one collectible unit; **Tone.js considered & declined** to stay vanilla/no-dep; supersedes the Notelings "instrument→creature-breed" channel; may rework the M6 Timbre gate) — the **look-only slice is BUILT 2026-07-20** (`SKINS` registry + `skinFor`; a `.cskin` child overlay in `cardHTML` — survives the fly/FLIP `cloneNode`; **piano = plain, guitar = Foil, bass = Holographic**, Frosted-Glass/Neon spares defined; pure-CSS, reduced-motion aware; per-skin `art` field = drop-in your-own-art override; shows wherever guitar/bass do = M6+/Free Play); the per-voice **synth `preset` (sound) half + a "Sound Collective" collection are BUILT 2026-07-25** — a data-driven `VOICES` registry + `playVoice()` (vanilla Web-Audio: osc stack + ADSR + optional lowpass sweep/tremolo) fills the reserved `preset` seam; **9 voices** (grand/pluck/roundbass core + collectible glassbell/reedorgan/neonsaw/warmpad/musicbox/vibraphone); each `INSTRUMENTS` entry carries a `voice`, sound paths route through `playVoice`; campaign stays curated via `CORE_INSTRUMENTS`/`instrumentsFor`, while **Free Play draws a shuffled palette** (`freePlayInstruments`, `buildDeck(…,isFree)`, capped at `MAX_TIMBRES`) so voices vary per run; **discover-by-hearing** (`catalogSound` in `playVoice` → `sounds` Set / `persist.sounds`) fills a **🔊 Sound Collective** panel (sibling to the Codex, ▶ sample playback). **Deferred:** per-voice art, drum/sfx/ambient families, an equip/loadout layer (Tips-shop tie-in), M6 counting distinct voices. Demoted slice-1 note-grid preserved in `mujicians-compose.html`. **Notelings** letter-creature + Bestiary collection/story layer is designed but tentative/not built. A **7-movement progression campaign** (pitch→rhythm→dynamics→melody→harmony→timbre→structure, Codex-gated, today's game preserved as **Free Play**, daily-capped) has **Phases 0–2 + Phase 3 Stage 1 + Phase 4 core built** (MOVEMENTS registry; Campaign/Free-Play mode select; per-movement select-cap, scoring-term, deck-instrument & threshold gating; `persist.progress`; **the whole M1→M7 arc is playable** — every scoring term wired, M2 **per-play durations (♩/𝅗𝅥/𝅝)** (one picker → `run.curDur`; Stage-1 rework superseded the earlier per-card/per-note model), per-play p/mf/f Dynamics, chord stacks (cap grows by movement), harmony/timbre blends, and **M7 Structure**: the loop accumulates per-run into one single-key song, **form scoring** rewards phrase-fingerprint restatement + an **A·B·A** return, and the **M7 gate** is `hasABA`; real per-mechanic advancement gates throughout); the **core rhythm/melody rework Stage 1 is BUILT (2026-07-18)** — a **single continuous timeline** (`run.loop.events[]` + a tick `cursor`; each play appends right after the last; bars are visual only; `TPB=24`, `LOOP_BARS=12`), **multi-select = always a simultaneous chord** (`handIsSequenced` deleted), a **stack cap that grows 1→2→3→4 by movement** (`maxSelect`=1,1,1,2,3,4,4), **per-play duration** (one ♩/𝅗𝅥/𝅝 picker → `run.curDur`, like Dynamics), a **rest card playable alone** (M2+), a **piano-roll** loop grid, a timeline scheduler, timeline-aware scoring (rhythmic-variety/dynamic-contrast/stepwise-motion/form over events), and an **event-list save format** (MJ2 codes; legacy `bars[]`/MJ1 still read); two small deviations — a minimal `detectTimelineRun` pulled forward so the M4 gate (cap 2) stays clearable, and a bar-number-ruler footer instead of per-event labels. **Stage 2 (not built):** 8ths/16ths/triplets in the picker (tick model already fits), fuller timeline run-detection & form scoring, save-migration polish, chord-inside-melody; plus Phase 4 *polish* (boss constraints, mentor prose, AABA/verse-chorus), **key change → Melody M4** (**accidentals → Pitch M1 now BUILT 2026-07-19**, ♮ boss deferred) — see the doc's "Continuous timeline + consistent stacking" and "Accidentals" sections. **Balatro-style card animations built 2026-07-17** (deal-in from a deck pile, play→shrink/fly-to-note-cell + bloom, discard→pile, hand reflow, animated Sort — overlay-clone + FLIP layer, art-agnostic, reduced-motion aware; score-juice/reskin deferred). **Dynamics = size built 2026-07-18** — a
selected card + its loop cell scale to the chosen loudness (p small / f big, **size = volume**), with a WAAPI
"swell" tween; docs' Notelings channel corrected so **register/octave = flight** (grounded vs. flying
same-initial creature: Ant/Angel … Elephant/Eye), freeing size for loudness.* | `mujicians.html` | [`docs/mujicians.md`](docs/mujicians.md) |
| **Critter Hunt** — a real-data **Murdle-style deduction game**: deduce which **animal** (=suspect) played which **instrument** (=weapon) **where** (=biome), on the classic **upside-down-L logic grid** with auto-X and **flip-cards** (fronts = emoji, backs = plain-language real facts) for the three categories; clues are mostly **true facts** (real habitats / Hornbostel–Sachs families / biomes) so solving teaches, plus a synth-backed **audio** clue. **Procedurally generated** (roll solution → true-clue pool → brute-force uniqueness solver → prune-to-minimal). The endgoal is always to **unmask Wormwood** — the timbre-thief disguised as one of the animals — via a **final determining "THE UNMASKING" clue** routed through the solved grid (win = **name the impostor**, Murdle-style). Win → keep a **playable sound card** (instrument timbre / animal percussion) toward feeding Mujicians' Sound Collection. **MVP BUILT 2026-07-27** (emoji visuals, N=4); Wormwood-unmasking endgoal **AMENDED 2026-07-28**. Spinoff of the Mujicians M6 Timbre boss; evolved from a bird-only "Birdle" then an animals-only 2-axis cut. | `critter-hunt.html` | [`docs/critter-hunt.md`](docs/critter-hunt.md) |

The repo also holds many undocumented tools/pages (ambigram generators, anagram/palindrome builders, an
IPA game, `typing.html`, `punctuators.html`, etc.). Those have no `docs/` entry yet — read the file
before changing it.

## Shared conventions (apply to all of the above)

- **"ship it"** — When the user says "ship it": **first run a consistency pass** (unless the commit is
  trivial/mechanical — a typo, formatting, a rename — where drift is unlikely), then `git add .`, then
  `git commit -m '<concise, accurate message reflecting what actually changed>'`, then `git push`. No
  confirmation needed for the commit itself — just do it. (End commit messages with the `Co-Authored-By`
  line per the harness instructions.)
  - **Consistency pass (before committing):** review this session's changes for stale/contradictory
    **code comments in changed files**, **`docs/*.md`** (files changed this session or that describe the
    changed code — e.g. BUILT/not-built status markers), and **`CLAUDE.md` + the `~/.claude` memory files**.
    **Auto-fix the obvious** stale wording as part of the same commit; **pause and ask** before committing
    only when a fix is ambiguous or a judgment call. (Scope is the changed surface — don't audit unrelated
    unchanged files.)
- **Vanilla, single-file, no build step.** Each game is one self-contained HTML file (inline CSS + JS),
  no frameworks, no bundler. Don't add dependencies unless a feature truly requires it (say so and why).
  Intentional exceptions are documented per-game (e.g. Inklings fetches a bundled local WordNet dictionary
  (`data/dictionary.json` + `data/inflections.json`), the Nouns-wing shelf index (`data/noun-books.json`),
  the verb-category map for stat ladders (`data/verb-cats.json`), the adjective→potion dumbbell map
  (`data/adj-attrs.json`, built by `build_adj_attrs.py`), room layouts (`data/rooms/*.json`), the heraldry
  blazon roster for the DEV-only Blazon Shield (`data/blazon.json`), the WordNet relation graph that powers
  the Curator's per-word relatives page (`data/wordnet-relations.json`, lazy-loaded on first curator open),
  the hand-written scenario frames + swappable noun list for the Excla Machine daily interjection puzzle
  (`data/excla-scenarios.json`),
  and `2of12.txt` — all local project files, no runtime third-party API).
- **Ask clarifying questions before non-trivial work.** For any meaningful change, surface the genuine
  design forks and get answers before writing code. Prefer a recommendation over an exhaustive survey.
- **Parse-check, don't build a test harness.** These are browser games with no test rig; sanity-check
  that the inline `<script>` parses and let the user verify behavior in the browser. Don't run headless
  node/vm harnesses for game logic.
- **Don't reorganize the file layout.** The site is served flat and pages are interlinked; game HTML
  files reference **shared assets by relative path**, so moving them breaks URLs, navigation, and asset
  loading. Notable shared assets: `Alpha.png` (Spin Nids + Inklings glyph sheet), `2of12.txt` (punctuators
  + Inklings), `Lumberjack_Jack.png` (Inklings player). Keep game files and assets where they are.

## Notes

- The git remote reports it moved to `github.com/exclaMachine/PunctuatorsGame.git` (origin still points at
  the old `CanvasPunctuatorsGame` URL; pushes redirect fine).
- A persistent memory store at `~/.claude/projects/.../memory/` holds cross-session feedback/preferences;
  its `MEMORY.md` index is loaded each session.
