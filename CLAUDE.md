# PunctuatorsGame — Repo Guide

This repo is a **single flat-hosted static website** (GitHub Pages): an `index.html` hub plus many
interlinked word-game and word-tool pages, shared CSS/JS (`nav.js`, `theme.css`, …), and shared data/art
assets. Several self-contained HTML games live here alongside the site's other tools.

## Read the right doc first

Per-game details live in `docs/` so this root file stays small and every session loads only what's
relevant. **Before working on a game, read its doc.** Keep that doc current in the *same* change whenever
you add/remove/modify a feature (treat the doc as part of the diff, not an afterthought).

| Game | Entry file | Doc |
| ---- | ---------- | --- |
| **Inklings** — word-collecting adventure (hunt letter-creatures, spell words at a desk, fill a library) | `inklings.html` | [`docs/inklings.md`](docs/inklings.md) · [architecture/maps/portability](docs/inklings-architecture.md) · [grammar systems/edu design](docs/inklings-grammar-systems.md) · [world atlas/capitals (M1–M6 all BUILT — layered data, a daily capital letter to hunt, the world board, spelling places at the desk (a Capitalized bench inks a country/capital/sea/lake/peak, pairing a country with its capital earns its flag), flags & continents: a **flagpole** bought with ink flies any earned flag (assignment lives on the placed instance, set by facing it + E), plus **continent milestones** at 25/50/100% of a continent's own spellable names paying ink + décor, and **board navigation**: arrows/WASD step to the neighbouring place by marching the grid, `[`/`]` + ◀ ▶ buttons sweep one layer west→east (geographic order, never alphabetical — that would leak redacted names), `1`–`7` frame the continents)](docs/inklings-atlas.md) · [farming/scripts](docs/inklings-farming.md) · [collections/curation/decoration](docs/inklings-collections.md) · [object placement/cozy square (0,1)](docs/inklings-placement.md) · [poetry/phoneme engine](docs/inklings-poetry.md) · [phoneme fishing (BUILT — the 2D articulatory-water cast is the sole UI; the Phonicon is the Sound Board's planned tile stock)](docs/inklings-fishing.md) · [heraldry/blazon shield](docs/inklings-heraldry.md) · [story companions](docs/inklings-companions.md) · [signal flags/omen mast (tentative)](docs/inklings-signal-flags.md) · [cipher menagerie (tentative)](docs/inklings-ciphers.md) · [sound-effect minibosses/shouts (tentative)](docs/inklings-shouts.md) · [diacritics/diacryptids (tentative)](docs/inklings-diacritics.md) |
| **Kaimoju** — Rampage-style kaiju that destroys buildings by typing romaji for kana/kanji | `kaimoju.html` | [`docs/kaimoju.md`](docs/kaimoju.md) |
| **Specimen Lab** (Exquisite Corpse) — Rummikub-style monster-parts card game on canvas | `exquisite-corpse-rummy.html` | [`docs/exquisite-corpse.md`](docs/exquisite-corpse.md) |
| **Spin Nids** (Ambigram Word Game) — rotatable ambigram-tile word game | `SpinNidIndex.html` | [`docs/spin-nids.md`](docs/spin-nids.md) |
| **Write. Right!** — sentence-building card game with an exquisite-corpse drawing twist | `WriteRight.html` | [`docs/write-right.md`](docs/write-right.md) |
| **The Prescriptivist's Gauntlet** — transcribe sentences under stacking linguistic "laws" | `Prescriptivist.html` | [`docs/prescriptivist.md`](docs/prescriptivist.md) |
| **Pitch Bird** — sing-to-fly voice game; pitch controls the bird's height (Flappy pipes + UltraStar karaoke). *In development, not yet shipped* | `pitch-bird.html` | [`docs/pitch-bird.md`](docs/pitch-bird.md) |
| **Augminotaur** — voice-controlled first-person horror level: trapped in a raycaster labyrinth with a creature droning an augmented triad, escape via a **beatbox fight** (offline mouth-verb detection — BOOM/TSS/**KA**/HOLD/HISS, no speech recognition, no network). *In development.* **BUILT:** M1 raycaster + grid-step movement, M2 `VoiceInput` module + calibration entry flow, M3 `AudioContext.currentTime` beat clock (`clock.js`, 76 BPM) driving a subtle downbeat wall-shading pulse, M4 **Movement 1** (call & response tutorial fight): the Augminotaur billboard (`sprite.js`, depth-tested horned silhouette) drums a 4-pattern ladder via a procedural kit (`drums.js`, kick/hat/snare = BOOM/TSS/KA), the player answers by voice, scored verb+timing against the clock (`movement1.js`); distance tug-of-war → win (triad resolves) / lose. The sprite is **swappable** (drop a PNG + set `CREATURE_SPRITE_SRC`/`setAugminotaurSprite()`; bull is just the default), and an **F-toggled dev readout** shows live per-hit grades + the expected pattern. Also a snare **"ka"** verb (mid-band detection; `voice.js` is the source of truth, the bench imports it — defaults pending mic tuning). Also **speaker-enrolled verb templates BUILT 2026-08-12** (`verbmatch.js`): MFCC+DTW ported from Kaimoju's voice-match probe — the player records their own BOOM/TSS/KA in a skippable in-flow enrollment step ("Speak in his tongue", `?enroll=1` to redo, `localStorage["augminotaur.verbTemplates.v1"]`), and templates then **fully replace** the tilt/mid-share attack classifier, which stays as the fallback (all-or-nothing: a partial set is never used). The onset detector still owns *when* and templates only own *what*, so hit timing is untouched — a 600 ms MFCC ring + a `pending` queue let a new onset arrive while the previous attack is still in its 110 ms classify window. Bench has an Enroll panel; `matchTh`/`margin` defaults pending mic tuning. Fixed alongside: nothing pumped `vi.update()` during the entry overlay, so **latency measurement never actually worked** (silently fell back to 0 ms) — `calibrate.js` now runs its own rAF pump. **Designed/tentative:** M2-of-fight the Groove + M3-of-fight the Seam, procedural maze + voice-only traversal. Input research bench: `augminotaur-input-bench.html`; brief in `HANDOFF.md`. | `augminotaur.html` | [`docs/augminotaur.md`](docs/augminotaur.md) |
| **Mujicians** — Balatro-style music-theory deckbuilder: cards are notes (ROYGBIV), "poker hands" are chords/scales/progressions, score = theory correctness, and every hand is played as audio (a high-scoring hand literally *sounds good*). **A run is one continuous, open-ended, single-key (C major) Mario-Paint-style performance** filling one accumulating loop; press **✓ Finish** (Free Play) or meet the movement's **per-run goal** (Campaign), then **Save-a-Song** (name/keep the whole-run song + theory report card; replay/export from a Home Setlist via `MJ2:` codes). Daily-capped. The **per-run Muse draft is currently disabled** (flag-gated `MUSE_DRAFT_ENABLED=false`; machinery/`MUSE_POOL` kept for a future re-enable). **Built:** a **7-movement campaign** (pitch→rhythm→dynamics→melody→harmony→timbre→structure), Codex-gated, with today's full game preserved as **Free Play**; a **continuous event-timeline** (`run.loop.events`, `TPB=24`, always-stack chords, piano-roll, `MJ2` save format via `snapshotEvents`); **per-card note values via the Break/Merge mechanic** (halve/double a card — teaches note & rest notation); **M1 Pitch** call-and-response ear-training on a gated ladder (naturals→by-ear→sharps→flats, `acc`-spelled enharmonic cards); **M2 Rhythm** as a live finger-drumming Beat Lab (kick/snare/hat, timing/combo, groove ladder; graduates to a "🥁 Lay a beat over it" pass on the unified grid); **M3 Dynamics** (p/mf/f, size = volume); chord stacks (M4–M7); **M7 Structure** form scoring (`hasABA`); Balatro-style card motion; a data-driven **`VOICES`/`playVoice`** sound engine + a **"Sound Collective"** collection (9 synth + 32 real sampled instruments + player-tuned animal voices from Critter Hunt); **collectible card skins** by Hornbostel–Sachs family. **Designed, not built:** the per-movement mentor/villain cast as earnable Muses (Dee/Sharpist/Sir Flatterer/Gaia/Crescendra/Cantrip/Mage Orc/Timbrewolf/The Key vs Ranger/Sandmar/Morendo/Slurry/Tritony/Wormwood/The Bartender); the **7 movement bosses** (one per modality — the M6 Timbre boss is **built as Critter Hunt**, a link-out Murdle game; standalone core-loop MVPs exist for Morendo `scorch-bones.html` + Tritony `forge-quench.html`); a Tips-based backstage shop; mobile-landscape support; the Notelings creature/Bestiary layer. The demoted slice-1 note-grid lives on in `mujicians-compose.html`. See the doc for full detail. | `mujicians.html` | [`docs/mujicians.md`](docs/mujicians.md) · [voice set-pieces (tentative): Beatbox Boxing](docs/mujicians-voice-boxing.md) · [Vowel Monkey](docs/mujicians-voice-monkey.md) |
| **Critter Hunt** — a real-data **Murdle-style deduction game**: deduce which **animal** (=suspect) played which **instrument** (=weapon) **where** (=biome), on the classic **upside-down-L logic grid** with auto-X and **tabbed, enlarged flip-cards** (fronts = emoji, backs = plain-language real facts, one category shown at a time) for the categories; clues are mostly **true facts** (real habitats / Hornbostel–Sachs families / biomes) so solving teaches, plus a real-sample (pitch-shifted) **audio** clue and **Murdle-style compound clues** (XOR "either/or but not both" ⚖️, narrow-to-two 🔀, if-then ➡️, size-comparative 📏 — **exactly one "unclear" compound per case**, generator-seeded + prune-protected + capped; best-of-samples toward ~3 clear + 1 unclear + the unmasking clue; `atomsFor`/`buildCompoundPool`). **Procedurally generated** (roll solution → true-clue pool → brute-force uniqueness solver → prune-to-minimal). The endgoal is always to **unmask Wormwood** — the timbre-thief disguised as one of the animals — via a **final determining "THE UNMASKING" clue** routed through the solved grid (win = **name the impostor**, Murdle-style). Win → keep a **playable sound card** (instrument timbre / real animal cry) toward feeding Mujicians' Sound Collection. **MVP BUILT 2026-07-27** (emoji visuals, N=3 — Murdle-matching, dropped from 4 on 2026-07-30); Wormwood-unmasking endgoal **AMENDED 2026-07-28**. **4th axis (Genre) BUILT 2026-07-31** — the engine is now axis-generic (anchored on animal via `partner()`/`pairHolds()`); each case `rollAcats()` appends an optional **`genre`** axis ~35% of the time → a **4-variable / 6-sub-grid** case (`renderBoard` draws the classic staircase generalised to K categories; `marks` matrix per `pairKey`; `GENRES` pool + `PAIR_REL` phrasing; still guaranteed unique/solvable, `TARGET_CLEAR` 3→5 for K=4; a `.k4` CSS shrink + `.gridscroll` keep it mobile-fit). Entities per category `AXIS_N` stays 3. **Animal sounds BUILT 2026-07-28; grown to 20 SPECIFIC species 2026-08-01** — all animals play real hand-curated vocalisations (`sounds/animals/<slug>.mp3`, Wikimedia Commons CC0/PD/CC-BY/CC-BY-SA, none ND; `fetch-animal-samples.sh` + `data/critter-credits.json`; `playAnimalSound` + a per-category `hear()` handler make animal cards *and* the won collection ▶-playable); how each animal becomes a Mujicians card is now a **player-tuned choice, not a dev per-animal decision** (BUILT 2026-08-01 — see below). **Every animal is now a SPECIFIC species whose clip is a recording OF that species so the name matches the actual call** (Red Fox, Rattlesnake, Great Horned Owl, Orca, Humpback Whale, …); the pool grew 10→20 (6 renamed to species, 4 ex-generic re-sourced — Frog→Banded Bullfrog, Elephant→African Elephant, Turtle→American Alligator 🐊, Dolphin→Orca 🐳 — plus 10 new: wolf/whale/mallard/peafowl/howler/turkey/goat/rooster/cow/sheep), added `omnivore` to `GLOSS`; no engine change (uniqueness falls back to the always-unique name). Card layer is data-driven off a **`CATEGORIES`** registry (tabs + enlarged cards); the **4th axis = musical genre** (real-data, not Murdle's "motive") is **BUILT 2026-07-31** — the whole engine (grid/solver/clues) is now axis-generic and cases roll 3 or 4 variables. Spinoff of the Mujicians M6 Timbre boss; evolved from a bird-only "Birdle" then an animals-only 2-axis cut. **Daily-game expansion Phase 1 BUILT 2026-08-02** — a **strict daily-only shared puzzle** (Wordle-style): a `mulberry32`+`hashStr` seeded `RNG` feeds the whole generation path (`rnd`/`shuffle`/clue-scoring/`rollAcats`) off the **local date** (`newGame` reseeds), so everyone gets the identical case that day; `localStorage["critterhunt.daily"]` locks it once solved (`markDailySolved`/`applyDailyLock` hides "New case", adds a 📅 Daily Case badge, disables Unmask + "come back tomorrow"); **boss mode (`?boss=mujicians`) stays endless/retryable** (`RNG=Math.random`, no lock). **Phase 2 (note/pitch clue) BUILT 2026-08-02** — `sampleCast` clones instruments per-case + stamps each PITCHED one (has `sample`; Drum/Conga skip) a distinct note from `PITCH_POOL`=C3·G3·D4·A4·E5 (`noteName`/`NOTE_NAMES`); `playInstrument` plays a card at `inst.pitch`; **pitch is an audio clue ref** parallel to timbre (`refsFor` emits `key:"pitch"`, `clueText`/`mk` thread a `note`, flows through single clues/compounds/`buildUnmaskClue`); a pitch clue in the Case File gets a **▶ the note** button that plays a **neutral triangle** (`playNote`) so you match by ear against the cards (the accessibility payoff); `pitchCluesOn` drawn once/case from the seeded RNG (`PITCH_CLUE_CHANCE`=0.55, so ~half of cases offer it; cards play their notes regardless). **Phase 3 (audio-first evidence layer) BUILT 2026-08-02 — real recordings only, NO synth** (dev call: synth beds/riffs sounded bad, removed): `playBiome`/`playGenre` play a curated **PD/CC0 recording** (`sounds/{biomes,genres}/<slug>.mp3`) or nothing; a sourceless axis is silent and its card ▶ / clue-cue is hidden (availability probed at load via `probeAmbience`/`hasClip`). Location & genre cards gained `hear`/`soundPath`; biome/genre clues carry a `▶ the biome/style` cue (only if `hasClip`); **🔊 Listen to the case** (`listenToCase`) plays every suspect→instrument(at its note)→biome→genre in turn. Sourced from Wikimedia Commons: biomes reef/forest/jungle/tundra/mountain/city/savanna (**desert silent**); genres jazz/classical/folk (**blues silent** — auditioned clip rejected as poor; **rock/electronic/disco/pop silent** — no PD/CC0 example); `fetch-biome-samples.sh`/`fetch-genre-samples.sh` (`SILENT` sentinel), credits in `data/critter-credits.json`+`sounds/CREDITS.md`. **Phase 4 (social) BUILT 2026-08-02** — a **shareable spoiler-free emoji result** (copy-to-clipboard): since the daily has no fail state (accuse until right), the skill signal is **wrong-guess count** — a `dailyMisses` counter (bumped per wrong `judgeAccusation`, reset in `newGame`) renders `❌…🟩`; `shareText()` regenerates from stored result+stats (names nothing — date, squares, `🔥 Streak`, `🧩 3/4-var`), `copyShare()` = clipboard API + textarea fallback; **streak/stats** in `localStorage["critterhunt.stats"]` (`{played,streak,maxStreak,lastSolved,dist}`) via `recordStats()`/`markDailySolved(misses,k)` (idempotent, `yesterdayStr()` streak, guess-count histogram); surfaced in the win modal (preview + 📋 Share + `statsSummary`) and the daily-locked state; boss mode records nothing. **Phase 5 (Mujicians Home button) BUILT 2026-08-02** — a **🔍 Critter Hunt** ghost button on the Mujicians Home CTA row (next to 🥁 Beat Lab, `b-critter`) opens `critter-hunt.html` (no `?boss` → today's standalone daily), distinct from the M6-boss `launchBoss` (`?boss=mujicians`). Daily-game expansion Phases 1–5 all BUILT. **Animal pixel-art sprites BUILT 2026-08-02** — a `faceHTML(e)` helper renders an `<img class="asprite">` (sized `1em`, `image-rendering:pixelated`) when an ANIMALS entry carries a `sprite` filename under `sprites/animals/`, else the emoji; shown everywhere a face appears (flip cards, grid heads/row labels, accusation buttons, win/collection cards). Per-entity migration via **`spriteSrc()`** (bare filename → `sprites/animals/`; a `/`-path → under `sprites/`, e.g. `instruments/bell.png`) — **13 animals** (orig 9 sheet-cut fox/mallard/rooster/goat/orca/cricket/bullfrog/howler/peafowl + 4 emoji-baked sheep/cow/wild-turkey/humpback-whale) **and 6 instruments** (emoji-baked trumpet/violin/drum/sax/bell/banjo, `sprites/instruments/`) wired; rest still emoji. **Emoji-baked sprites** come from **`bake-emoji-sprites.py`** (Pillow renders the Apple Color Emoji 160px strike → 40² grid, 8-level posterize, hard-α → tiny 40×40 PNG upscaled nearest-neighbour; recipe chosen interactively in the **`emoji-pixelizer.html`** dev tool — repo root, opened from Critter Hunt's `?dev=1` panel; dynamic: add any emoji or drop your own image files, tune grid/posterize/levels, download a game-ready PNG into `sprites/{instruments,animals}/`; `bake-emoji-sprites.py` is the CLI batch equivalent; frozen at Apple's emoji, the tradeoff for cross-platform consistency). **Cozy-retro pixel restyle BUILT 2026-08-02** — chrome-only pass: hard 90° corners, chunky dark-warm outlines, offset block shadows (no blur), warm palette kept; a bundled bitmap font (**Pixelify Sans**, OFL, `fonts/PixelifySans-latin.woff2`, `--pix`) on chrome only + a legible sans (`--sans`) for all body/clue text (phone legibility); emoji unchanged; the instrument tab/card label reads **Instruments** (not Murdle's "Weapons"). Critter Hunt only. | `critter-hunt.html` | [`docs/critter-hunt.md`](docs/critter-hunt.md) |
| **IPA Scrabble** — an Inklings word game where you **spell by sound, not letters**: tiles are **IPA phonemes** and a play is valid when the laid phoneme sequence matches **some real word's pronunciation** (`/t u/` = two·too·to; homophones collapse). **BUILT 2026-08-08** — a standalone dev file, shipped first as a rack word-builder then upgraded the same day to a **full 15×15 Scrabble/WWF board** with **play-off-others**: **full cross-word rule** (a play forms a main line + every perpendicular cross-run it touches, and **each run must be a valid pronunciation**), standard placement (single line, no gaps, first word crosses the ★ center, later plays connect to the board), **full premium squares** (DL/TL/DW/TW, standard layout, center double-word, **BINGO +50**), solo score-attack. Click-to-place UX (`evaluateTurn`/`runAt`/`cellTile`/`scoreCells`; `pending` vs committed `board`); live per-run status gates **✓ Play**. **Keyboard placement BUILT 2026-08-09** (select→aim→place): `1–7`/click select a rack tile, a cursor visits only **valid squares**, arrows/`Tab` move it, `Enter`/`Space` place, `Backspace` undo, `Esc` deselect — no orientation to manage (`validTargets`/`moveCursor`/`cycleCursor`/`placeAt`). **Single-line targeting (2026-08-09):** `validTargets` now keeps a turn's tiles on one contiguous line as you lay them (1st tile → anywhere the rack can still reach the board from, see next; 2nd → the 1st tile's 4 neighbors, locking the axis; 3rd+ → extend either end, bridging over committed tiles, + interior gaps), and **click is held to the same targets** as the cursor (drop only on a highlighted square) — replacing the old "adjacent to any letter" / "click anywhere" behavior. **Reach-based targeting BUILT 2026-08-14** — it's the *play* that must connect, not the tile you lay first, so the 1st tile is no longer pinned to a square touching the board: reach = **the number of tiles you hold** (7 in hand → start up to 6 squares out and hook the board with your last phoneme), committed tiles inside the span bridge for free, and *every* candidate square is pruned unless the play can still reach a hook with the tiles left after it (no stranding). `isHook` (★ center on an empty board, else an empty square touching a committed tile) + `canStillHook(cells,dr,dc,remaining)`; the board shows two tiers — solid outline for squares that hook on their own, faint (`.cell.target.far`) for reach-only. **Fixed 2026-08-09:** when your first tile of a turn connects to an existing committed letter, the 2nd tile can now continue that word's line — `validTargets` treats the abutting axis as set and offers its bridged extension (over committed tiles) alongside the four neighbors. Fetches `data/ipa-pronunciations.json` (**49,947 Scrabble-valid words** → IPA, 39-phoneme inventory; built by `build-ipa-pronunciations.js` from the full CMU dict `text-to-ipa-master/lib/ipadict.txt` ∩ `enable1.txt` — **replaced the curated `IPA-fan-game/ipa_words.js`**, which had no ≤3-letter words) → **needs http serving** (Live Server); tile point-values + bag distribution are **data-driven from real phoneme frequency**. **Sound FX BUILT 2026-08-09** — a tiny procedural WebAudio chiptune engine (`SFX`, no assets, works `file://`, matches the 8-bit look): select/place (pitch rises as tiles stack)/pickup/error blips, a valid-word arpeggio (bigger BINGO fanfare), shuffle/trade swooshes, hint sparkle, new-game jingle, game-over cadence, all behind a **🔊/🔇 Sound** toggle persisted in `localStorage`. **Sound-wordplay bonuses BUILT 2026-08-14** — a played run that's a **phonetic palindrome** (`/b ɪ b/` bib, `states`, `canonic`) pays **+10 per phoneme**, one that reverses into *another* real word (a **semordnilap**: `stop` ⇄ `pots`) pays a flat **+10**; detection is a **live check** (`wordplayFor`: reverse the run's tokens, compare / test `VALID`) so no extra data file loads — the prebuilt `data/ipa-palindromes.json` + `ipa-semordnilaps-*.json` stay build-time artifacts — with a **3-phoneme floor** (`WORDPLAY_MIN`) dropping trivial 1-phoneme and cheap 2-phoneme mirrors; every run formed (main + crosses) is eligible, the bonus is a flat add after premiums (like BINGO), and it's surfaced three ways: a gold **live preview tag** on the status chip *before* you commit, a **plain-language banner** naming the wordplay + a dedicated **mirrored-arpeggio SFX** (`SFX.palindrome`/`SFX.semordnilap`), and **↔/⇄ tags + a dashed bonus line** in the Word Hoard with an **↔ WORDPLAY** statbar counter. Launched from Inklings' **DEV badge** (dev-only; not yet folded into `inklings.html`). **Production build (the endless Sound Board) PLANNED 2026-08-14, doc §10; M1 BUILT 2026-08-15, M2 BUILT 2026-08-17, M3 BUILT 2026-08-17 (playable in Inklings now, as **the Sound Board** — the dev-settled in-game name)** — folded into `inklings.html` as a toolbar overlay (a world bench later, launcher-only change) where the **bag is your fished Phonicon**: the rack is drawn from `state.phonicon` weighted by count and **laying a tile destroys it** (`count` *is* the tile stock; only laid tiles are spent), needing two aliases (data uses `ɡ` vs the inventory's ASCII `g`; **`ʌ` appears in zero pronunciations** — CMU folds it to `ə` — so `ʌ`→`ə` tiles). The board becomes **endless & persistent**: premiums are a *pure function of position* (`premiumAt` = standard 15×15 layout indexed `r%15,c%15`), so it's infinite in all four directions with **no stored panel state** and "extend the board" is viewport work only; one saved crossword you add a word or two to per day, with a **chain multiplier** (+0.1 per connected play, uncapped, survives across days) that **🌱 New patch** resets when you choose to lay somewhere unconnected. Adds **articulatory premium squares** (◆ VOWEL ×3 · ≈ FRICATIVE ×3 · 🔊 VOICED ×2) built off the `place`/`manner`/`voice` data fishing already authored, an **ink-priced ♻ Trade** (2 ink, +2 per further trade that day), and per-play payout of **ink + Word Hoard words** (no game over). Save v11→v12; pronunciations lazy-loaded (1.4 MB); `ipa-scrabble.html` stays the engine bench. **M1 (data & stock plumbing, headless) BUILT 2026-08-15** in `inklings.html` — a `/* THE SOUND BOARD */` block holding the lazy `loadPronunciations()` (→ `SB_VALID`/`SB_WORDS`/`SB_TILE`, values derived from the lexicon's own phoneme frequency), the verified `SB_ALIAS` (`g`→`ɡ`, `ʌ`→`ə`; all 40 fished sounds playable, all 39 data tokens fishable), stock helpers over `state.phonicon` (`sbStock`/`sbStockList`/`sbDistinctPlayable`/`sbSpend`) and the count-weighted, without-replacement `sbDrawRack`; a spent-to-0 Phonicon card reads "out of stock" instead of re-locking. Nothing spends until M4. **M2 (the endless board model, headless) BUILT 2026-08-17** — a second block holding the whole board engine: **`sbPremiumAt(r,c)`** reads one standard 15×15 `SB_PREMIUM` table at `(r%15,c%15)` so the grid is infinite by construction (no panel state to store; the ★ exists once, at absolute `(7,7)`), with **articulatory squares** (◆ VOWEL ×3 · ≈ FRICATIVE ×3 · 🔊 VOICED ×2) resolved in the same call — `mulberry32(hash2(panel))` converts **6 of each panel's DL/TL squares** (never a word premium, never the ★), and `sbArtPays` reads the *same* `data/phonemes.json` `manner`/`voice` fields fishing authored, so a non-matching tile just scores face value. Board state is sparse on `state.soundboard` (`{tiles:{"r,c":tile},count,bounds,score,chain,patches,patchFree,…}`, **not yet in `snapshot()`** — save stays v11 until M4); the bench engine is ported onto it (`sbCellTile`/`sbRunAt`/`sbScoreCells`/`sbWordplayFor`/`sbEvaluateTurn`/`sbValidTargets`/`sbIsHook`/`sbCanStillHook` + turn state `sbRack`/`sbPending`/`sbFillRack`/`sbRecall` and `sbCommitPlacement()`, which writes tiles + grows bounds — M4 layers spend/chain/ink/Hoard on top). One thing couldn't be ported literally: the bench scans all 225 squares to find first-tile targets, which on an endless board grows with explored **area**, so **`sbReachFromHooks(budget)`** enumerates the identical set backwards — outward from each hook, up to `budget` empty squares, bridging committed tiles free — making the cost board *tiles*, not board *area*. `patchFree` (M4's 🌱 New patch) is honoured throughout. **M3 (the overlay) BUILT 2026-08-17** — the third block plus the `#soundboard` markup/CSS makes it playable: a **viewport** onto the infinite grid (`sbView={r0,c0,rows,cols}`, `sbLayout` sizes it from the window — 32 px cells desktop / 24 px phone, ≤19 either way — because the mobile breakpoint turns the panel into a scrolling block where measuring reads 0), panned by **drag**, by **arrows when no tile is picked**, by the aim cursor pushing the edge (`sbEnsureVisible`), or by **⌖ Recentre / `C`** (`sbHome()` = last play → middle of what you've built → the ★); `sbClampView` holds the window against the playable region so you can't pan into void, and squares past the frontier render dimmed. The bench's **select → aim → place** flow ports unchanged (`1`–`7`/tap pick, arrows aim over valid squares only, `Tab` hops in reading order, `Enter` lays then plays, `Backspace` undoes, `Esc` backs out a layer at a time), with the two target tiers drawn solid (hooks on its own) vs faint (reach-only). Because the board re-renders wholesale, there are **no per-cell listeners**: `sbCellFromEvent` derives the cell from the viewport rect and one pointerdown/move/up on `#sb-view` splits tap-to-place from drag-to-pan (7 px = a drag). Live per-run status + the gold wordplay preview gate ✓ Play. Still **economy-free**: ✓ Play commits the word and moves the board's score/word counters, but nothing is debited from the Phonicon, the chain never grows, no ink/Hoard payout, nothing saved (all M4, save v11→v12). **♻ Trade is free in M3** (dev's call, so a phone test can't dead-end on an unplayable rack) — M4 prices it in ink; **🌱 New patch** stays M4. Below 2 distinct playable sounds the overlay says *go fishing*; an aliased tile's tooltip says what it was fished as. Launched from a non-contextual toolbar row (`tb-board` 🔡) + touch button (`tc-board`) via `tbSwitch`, with `state.soundboardOpen` added to all six play guards, `closeAnyDialog` and `musicDialogueOpen`; the Controls panel gained a Sound Board paragraph + key row. The **articulatory-square explainer**, the Fish Phoneme Guide pointer and the ported wordplay SFX are M5. This **amends fishing's "sounds only" rule** — fishing still pays no ink, but the loop becomes fish → board → ink. Still deferred: AI opponent, wildcards, daily/shared board. | `ipa-scrabble.html` | [`docs/ipa-scrabble.md`](docs/ipa-scrabble.md) |
| **Punctuators: The Game** — the site's own game: type a sentence, then shoot its punctuation back in with a team of punctuation-mark heroes; a dropdown swaps the punctuation hunt for **wordplay modes** (Ambigrams, Anagrams, Homophones, Alphabet Slots, …), each mode being one `<option>` + a `wrap*` span-marking function + a branch in `animate()`'s collision block. **The game itself still has no base doc** — read `index.js` / `utils/utils.js` before changing it, and read [`docs/punctuators.md`](docs/punctuators.md) first: it holds the **hero-placement rule across a Switch Character** (FIXED 2026-08-23 — the incoming hero inherits the outgoing hero's *centre*, clamped by `clampHeroX()`; both recentring and per-hero position memory were wrong) and the **two shared-engine footguns** that silently empty the team and white-screen every mode — `waitForElement()` must wait on `#output span`, never a bare `span`, and its observer must read every mutation record, not `mutations[0]`. Also the **centred sentence** (BUILT 2026-08-25, CSS only, all modes): `#output` is fixed with no `left`/`right`, so it shrink-wrapped its text and a short sentence sat in the far-left corner with the hero walking the whole width to reach it — `left/right: 0` + `text-align: center` centre **each line individually** (a short wrapped line stays under the middle), horizontally only (`top: 110px` stays — that gap *is* the projectile's flight time), and the 480 px override needs its `max-width` moved in step with its 10 px margins or the over-constrained box drops `right` and sits 40 px off-centre. | `punctuators.html` (+ `index.js`) | [`docs/punctuators.md`](docs/punctuators.md) *(known issues + engine footguns; not yet a full base doc)* · [General Ization & Keen Arrow — the Word Ladder **(FREE PLAY IS PLAYABLE — M1 data BUILT 2026-08-22; M2 the mode + M3 the heroes + M12 the Tree of Kinds map + M13 its fill + M14 its feel BUILT 2026-08-23 — **the map is complete**; M4 the feel completed 2026-08-25 with the hero art, bar a second attack frame)**: two heroes sharing one mode and one set of target words, climbing WordNet's is-a-kind-of hierarchy — General broadens (`poodle→dog→mammal→animal`), Keen Arrow narrows, **Switch Character flips the direction**. The `Hero.targetId` split that lets two heroes answer to one span id is **BUILT** — four one-line edits (`Hero` constructor, `heroToTheRescue`, the `animate()` collision gate, plus `ladderDirection` on the two new classes); `targetId` defaults to `symbol`, so all 23 existing heroes are untouched. **M1 = the data**: `build-ladders.py` (offline, NLTK WordNet, downloads to a temp dir) emits `ladderPOJO.js` — **30,545 words / 4,837 parents / 337 KB**. The raw `wordnet-relations.json` is sense-flattened (`tree→actor`, `dog→sausage`) so rungs are sense-disambiguated per word; three findings changed the plan: (a) sense scoring is **corpus-count-first**, `_SENSE_RANK` only a tiebreak among zero-count senses — the reverse of `build_dictionary.py`, whose rank-first order gave `king→checker`, `soldier→insect`, `drum→fish`, `jacket→peel`; (b) the climb is **breadth-first over every hypernym branch**, since `hypernyms()[0]` buries `mammal` under `domestic_animal` and makes a car a kind of `container`; (c) a rung must be **sense-coherent** (its own best sense *is* the synset it names), else the chain derails one rung later (`mountain→elevation→plan→drawing`). Ships **`ladderDown`** (parent → space-joined children) not `ladderUp` — 337 KB vs 694 KB, inverted at load; the sibling list Keen Arrow's deferred branching-hyponym idea wants is then the raw shipped form. Tuning lives in `TOP_STOPS`/`MID_RUNGS`/`BANNED_RUNGS` + tiny `PARENT_OVERRIDE`/`SENSE_OVERRIDE` tables; `--spot`/`--why`/`--check` flags drive the tuning loop. **M2 (the mode) + M3 (the heroes) BUILT 2026-08-23 — free play is playable**: a `General & Specific` `<option>`, `ladderFunc.js` (`loadLadders`/`hasLadders`/`wrapLadders` + the rung lookups), `protectedLadders` in `SpanPlaceholder.js`, a `case "ladder"` in `addSpansAndIdsForWordPlay`, the two hero classes adjacent in `availableHeroArray` (placeholder art at first — `Generic.png` for General and Question Markswoman's own `qm.png` for Keen, which made the two heroes identical on screen; **real art BUILT 2026-08-25**, `images/Ization.png` + `images/KeenArrow.png`, in both hero slots since there is no attack frame yet, with `heroScale` 0.45 for both (same 800×1045 files, but the *figure inside the frame* differs — 798×875 vs 442×663 — so a shared scale draws Keen the smaller of the pair, ~359×394 vs ~199×298) and, **since 2026-08-27, a `projectileAnchor` rather than `projectileStartPositionX`** — every projectile was born at `player.position.y`, i.e. the top of the hero's **image frame**, which is the weapon only when the art fills its frame, and Keen's figure fills 442×663 of 800×1045 with `restingY` bottom-anchoring the rest, so her arrow launched **211 px above the crossbow**; a `projectileAnchor {x,y}` offset measured off the art's alpha (Keen `{170,211}` = the loaded bolt's tip, General `{303,76}` = his raised blade, whose error was horizontal instead — his frame top *is* his sword tip but the default x is the midline) now feeds one `Hero.projectileSpawn()` that the two shoot handlers **and** `Projectile`'s `onload` all call, so a spawn is computed in exactly one place; `projectileAnchor` defaults to `null` and each site keeps its historical arithmetic in that branch, leaving the other 21 heroes bit-for-bit unchanged and `projectileStartPositionX` (180) inert for these two — which also retires the old footgun for anchored heroes, since the handlers' `x + width - startX` and `onload`'s `x + startX` agree at half the drawn width and nowhere else — borrowed SFX), and a `climbLadder()` branch in `animate()`'s collision block. Two spec reversals: the corpus is **lazy-loaded** (`await loadLadders()` = a dynamic `import()` in the now-`async` Pow! handler, sharing one cached module with `ladderMap.js`, so a visitor who never picks the mode pays 0 of the 337 KB — the static-import decision existed only to keep the wrappers synchronous, and they still are), and **`data/inflections.json` is NOT used** (two regular-plural rules in `ladderFunc.js` instead, so the mode carries exactly one data file; irregulars like `mice` simply never light up, which is the right failure). The span's authoritative state is **`data-ladder-word`**, not `data-ladder`/`data-rung` — those are *recomputed on every landing*, because sibling cycling means the path down is chosen shot by shot; case is copied by **shape** (ALL CAPS / Leading cap), not Betar's position-by-position `matchCase`, since rungs differ in length. §2.4's **rung strip** (`▲▲●▼`) was pulled forward from M4 as an absolutely-positioned `::after` reading `attr(data-rung-strip)`, so it never joins the line box or changes the span's hit rectangle. **Sibling cycling (§13.7) shipped with it and was then REMOVED 2026-08-23** once the shelf fan made it redundant — but its **unvisited-first** rule survived by changing jobs: it now orders the fan's row (`ladderMapHas()` read back out of the map), so **the Tree of Kinds is still an input to the game, not just a scoreboard**, and *replaying* is still what fills a wide shelf (a 2nd pass over `dog` brings up `watchdog`/`sheepdog`/`pug`, a 3rd `mutt`). **The shelf fan (§2.5) BUILT 2026-08-23**, added to M4 because playing M3 showed polish alone can't fix what it was for: Keen's descent still reads as *random*, since two siblings carry an identical rung strip and one shared flare, so a lateral hop looks exactly like a narrowing. The fan draws the word's narrower kinds as a **row of shootable words beneath it** — horizontal because projectiles fly **straight up with no aiming** (`velocity {x:0,y:-10}`), so a word's x-range *is* its selectability and a tree/arc layout literally cannot be aimed at. Contents are tiered off measurements: median shelf is **2** children (80% ≤6) so most shelves show whole, but the words people type are the tail (`dog` 33, `tree` 107, `fish` 221, `person` 805) — those take **branch-children first** (only **13%** of children are themselves parents: `dog` 33→8, `fish` 221→8), except **69.8% of parents have zero branches**, so buds top the row up and the rest is **fogged as `+N more kinds`** (§13.5's rule, so no leaf name is ever printed as a readable answer); the **last slot is always a rotating unvisited bud**, or 25 of `dog`'s children would be permanently unreachable and §13.7's fill would re-break. **Keen Arrow goes DOWN OR NOT AT ALL** (dev's call, 2026-08-23): `shelfFor` returns children or nothing, never siblings, because one hero's one action must not mean two things — narrow, or shuffle along the shelf you're already on — which are indistinguishable once drawn. The sideways move still exists via the honest route (broaden to the parent with General, narrow again; the parent's row *is* the sibling list), and §13.7 is unaffected because shelves fill from the parent's row. A leaf clanks: 84% of corpus words, but far fewer in practice since typed words are usually mid-chain. Row width is measured at draw time (~11 desktop / ~5 phone), capped 7 / floored 3. As built: `shelfFor` in `ladderFunc.js` (which is where `nextUnvisitedRung`'s unvisited-first rule moved — it now picks the row's *contents*, not the player's move), `openShelfFan`/`closeShelfFan`/`pickRung`/`landOnRung` + a `data-ladder-child` split in the existing ladder collision branch in `index.js`, `.shelf-fan` in `index.css`; closed on hero switch, new sentence and resize. **Four footguns, all hit:** **`nodeArr` is filled once and the observer then disconnects**, so fan children are never targets unless pushed in by hand; they must be **spliced out on close** or a removed node's all-zero `getBoundingClientRect()` becomes a phantom hit-box in the corner (and because removal and the collision walk interleave, `animate()` now also skips any entry failing **`isConnected`** — a guard that protects every mode); **one ladder action per shot must be latched at the collision loop, not in the handlers**, since `pickRung` opens the next row inside the same call and `forEach` walks to the length it captured, so the shot would otherwise register twice and splice itself out of `projectiles` twice; and the fan must be **absolutely positioned** like §2.4's strip or the first hit reflows every other word's target box. **The typed-answer hard mode is explicitly out of scope** — it's §12.2's Word Race input model and would mean writing that traversal engine early. **The How-to-Play copy BUILT 2026-08-23** — a `ladder` template in `updateCharacterModal` (Betar's lead → worked example → tips pattern) leading with the rule the fan made unguessable, *Keen Arrow doesn't move the word*, then glossing the `▾` marker, the rung strip, the `+N more` fog + why replaying changes the row, the clank's way out, the capstone and the 🌳 map; it writes into `.modal--body` instead of the whole `#modal`, which had been eating the header and its `×` (Betar's modal was overlay-closable only). **§6's two landing animations BUILT 2026-08-23** — a swap rebuilds the span as an in-flow `.ladder-face` (the new rung, holding the box so the sentence reflows once and never again) plus an absolutely-positioned `.ladder-ghost` (the rung being left behind, free to swell or shrink without moving a neighbour), driven by WAAPI in `animateLadderSwap`: General's **camera pull-back** (the shot word shrinks to a point while the broader one fades in oversized and settles) and Keen's **lens snap** (the broader word swells and blurs out, the narrower one drops into focus a beat later with a hair of overshoot), plus `flyPickedChild` carrying the child you shot out of the fan's row and into the word — as a **fixed clone**, since `closeShelfFan` has already spliced the real one out of `nodeArr` and a second hit must be impossible. The two no-move moments (`.ladder-capstone`, and the new `.ladder-aperture` flicker when the fan opens on a word that stays put) are **`drop-shadow`, not `text-shadow`**, which had been blanking the word's black outline for the length of the flare; `openShelfFan` gained a `pulse` flag so `pickRung`'s re-open doesn't fire the flicker on top of the lens snap. **The trap §2.5 created:** a second shot can land mid-swap, so a `dataset.ladderSeq` token makes a superseded `settle()` a no-op and `landOnRung` reads what's on screen off `.ladder-face` (mid-swap the span's own `textContent` is face+ghost run together). **General's projectile BUILT the same day — a drawn broadsword** (the joke is in the name; it beat §8's "spreading ring" on legibility at 27 px): `drawBroadswordSprite()` paints it once into an offscreen canvas and hands the Hero constructor a **data URL**, so it takes the same `Image()` path as every other projectile and swapping in real art is one string (`GENERAL_PROJECTILE`); drawn point-up (projectiles are never rotated) and sized so the unchanged 0.2 scale lands at the ~27 px width `projectileStartPositionX` was centred against. **§7's six SFX BUILT 2026-08-23** on the existing `_tone`/`_noise` kit, no new assets: `_izoShoot` (a bugle-ish rising third over an octave-down body — the harmonic series is most of why a bugle call reads as military), `_izoHit` (two voices starting together and pulling *apart* as they fall, so the interval widens on the way down — the pull-back as sound, with a 2 Hz detune so a low pad isn't one flat organ note), `_keenShoot` (a high-Q 220 Hz noise band that thrums rather than hisses, + the arrow's blip), `_keenHit` (a 45 ms tick, the shortest cue in the game), `_keenFan` (**one tick per child drawn**, so a three-word shelf and a seven-word shelf are audibly different *before* you read the row; takes a `delay` for the re-open case) and `_ladderCapstone` (a bright major triad shared by **both** ends — it must not sound like a buzzer, since `animal` IS the answer and a leaf having no kinds is a fact, not a miss). **The wiring is the part worth knowing:** six cues across four outcomes don't fit the generic one-hit-one-sound `player.hitProjectileSound()` call site every other hero uses, so **both ladder heroes override `hitProjectileSound()` to silence** and the ladder branch plays the outcome's own cue (`landOnRung` picks by `ladderDirection`; `openShelfFan` plays `_keenFan(children.length)`; `climbLadder` plays the capstone at both dead ends) — `openShelfFan`'s `pulse` flag turned out to carry the audio timing too, being false exactly when a landing just happened and the flutter should wait a beat. The borrowed `whoosh.mp3`/`featherSwish.mp3` were dropped from both constructors, matching how every other synth hero passes `undefined` there. **All that's left in M4 is a second (attack) frame per hero.** **Phase 2 — Restore the Phrase — SPECCED 2026-08-22 (doc §11); M5 the data BUILT 2026-08-22; M6 the mode BUILT 2026-08-24, DEV-ONLY behind `?dev=1` (same `data-dev` gate as Word Race) — it is PLAYABLE; M7 the daily and M8 the feel remain**: a daily puzzle showing a famous saying with words already shifted along the ladder (*A canine is a person's best friend*), restored by shooting them back — Punctuators' first wordplay mode with a real win state. Four decisions locked: an **authored** `phrases-source.txt` with **generated-then-frozen** shifts. **M5 (the data layer) BUILT 2026-08-22**: 108 drafted public-domain sayings where `{braces}` mark shiftable words (`{word>child}` pins the narrower rung, `{word^}` forces broaden, `{word+}` marks an invisible plural, `{{word}}` shifts 2 rungs), plus **`build-ladders.py --phrases`** → **`phrasePOJO.js`** (108 puzzles, 24 KB; 97 words broadened / 52 narrowed / 39 puzzles needing both heroes). `--phrases` reads the shipped `ladderPOJO.js` instead of re-climbing WordNet — so a puzzle can't assert a rung the game lacks, and it skips the NLTK setup entirely (a rebuild is ~1 s). It also **rewrites machine-written `#~` annotations** into the source under each phrase — the available rungs per braced word plus a `#~ =` line showing the puzzle it made, which is what the dev prunes against. **Open: the dev's sense-prune**; **daily headline + endless practice** behind one `PRACTICE_ENABLED` flag (destination is daily-only); **no fail state** — wasted shots are the score and the share stat; **its own `<option>`**, not a toggle inside free play. The load-bearing constraint is **sense vetting**: a word's ladder follows its own globally-best sense, which often isn't the saying's (`chicken → meat → food`, `needle → leaf`), so a human marks which words may shift and the script only computes the shift. **M6 (the mode) BUILT 2026-08-24 — playable, dev-only.** `ladderPhrase.js` (a departure from §11.8's "put `wrapPhrase` in `ladderFunc.js`" — the mode also wants a lazy corpus load, a phrase pick, the goal arithmetic and a run object, which is `ladderRace.js`'s shape, so it got `ladderRace.js`'s treatment), a `ladderPuzzle` `<option>`, article/Foon suppression in `utils/utils.js` (the shared flag, alongside Word Race — **renamed `preMarked` → `ladderMode` on 2026-08-25 when free play joined it**, since free play is *not* pre-marked, it just wants the same two heroes gone; suppressing Art the Tickler leaves the article with no hero to toggle it, so `landOnRung` now makes the `a dog` → `an animal` text-node edit itself via `fixArticleBefore`, shared by free play and the puzzle), and the goal/lock/win check. **The mode reuses the FREE-PLAY SPAN OUTRIGHT** — same id, same `data-ladder-word`, plus `data-phrase-slot`/`data-phrase-goal` — so `climbLadder`, the shelf fan and both §6 animations drive it and there is **no second ladder path through `animate()`**: the whole puzzle hangs off two hooks in the existing one (a pin for the fan, a scorer on the landing). **Three decisions the build settled.** (a) **Keen fans rather than walking the authored chain** (dev's call, revertible): §11.6 predates §2.5 and assumed the chain was a rail, but with the fan the player can walk **off** it, so the chain became a **score-keeper** — distance is measured on the live hierarchy (`rungsBetween` = §12's `parFor`) and General broadening back up is the way home. That forced **`shelfFor`'s new `pin`**: shelves run far wider than the row (`food` has 239 children, `eggs` is the 164th) and **77 of the 97 narrowing steps in the 108 puzzles pass through a shelf wider than a 7-slot fan**, so without pinning the answer into the row most puzzles would be *unsolvable*, not merely hard. The pin displaces the row's last word and the row is re-sorted into corpus order so the answer has no tell-tale slot — though **MEASURED: the answer is the row's first word 48% of the time anyway**, which is the data (sayings use the most familiar kind), not the pin. (b) **A wasted MOVE, not a wasted shot, is the score** — the fan makes a descent two shots and §12.8 already settled that the extra shot is free. (c) **A solved word locks** by having its `id` cleared, which also makes the Hint button light only what's left, for free. Also: **M6 deals a random puzzle every Pow!** rather than the hardcoded one the milestone planned (M6 *is* the practice path, and sweeping the corpus at random is how the phrases still wanting the prune get found), the live `A dog` → `An animal` fix is a **text-node edit** (Art is suppressed, so the articles are plain text) that is spelling-based not phonetic, and `_phraseLock` is the **eighth** ladder cue — a latch, deliberately unlike the capstone, which means an *end* rather than an answer. Verified before it ever ran in a browser: all 149 braced words wrap, all 108 puzzles solve by landing every goal, every narrowing step's answer survives into a 7-slot fan, and live distance agrees with the authored chain on every fix. **Phase 3 — Word Race — SPECCED 2026-08-22 (doc §12); **M9 (the engine) BUILT 2026-08-24 + M10's PAIR POOL BUILT 2026-08-26, DEV-ONLY while it's worked on** (the `<option>` carries `data-dev` and is stripped from the dropdown unless the page is opened with `?dev=1`, gated in `punctuators.html`'s dropdown IIFE *before* the custom dropdown is generated from `sel.options`, since hiding the `<option>` would leave its generated `.custom-select-option` visible). **M10's PAIR POOL BUILT 2026-08-26 — the hardcoded `poodle ⟶ salmon` is gone**: `race-pairs-source.txt` (hand-authored, one `start target` per line) + a new **`build-ladders.py --pairs`** pass → **`racePOJO.js`** (239 pairs / 12 trees / 7 KB / par 4:159 5:72 6:8 = 34 weeks of dailies), plus `loadRacePairs`/`raceDayIndex`/`pickRacePair` in `ladderRace.js` and `RACE_DAILY`/`chooseRacePair` in `index.js`. **It deals a RANDOM pair per Pow! and re-rolls whenever you pick the mode in the dropdown** (practice, exactly as §11's M6 does — sweeping the pool at random is how the pairs still wanting a prune get found); `RACE_DAILY = false` is the **one flag** between that and the positional `pickRacePair(raceDayIndex())` daily, and `?race=N` forces a pair by index. **Two spec bullets were measured and REVERSED before a line was written:** §12.3's pool of "18,863 words in `2of12.txt`" is far too loose for a daily — it deals `thole ⟶ menorah`, `pectin ⟶ pepsin`, `percolator ⟶ majolica`, all of them *in* 2of12, and a route-familiarity filter barely helps because 98% of same-tree pairs already pass it — and the harder-Sunday band went too (par 7+ is 750 of 96,000 candidates in the concrete trees and skews obscure: `sitter ⟶ bumblebee`). **The reason it's a hand-kept source file** and not pure generation: `poodle`, `canoe`, `villa` and `beagle` all carry a SemCor count of **zero**, and so do `zwieback`, `demijohn`, `wain` and `thole` — every automatic filter that excludes the second set excludes the first, so the dev prunes (§11.4's pattern) and **`--pairs --suggest N`** does the machine half, appending route-annotated candidates. `--pairs` needs **no WordNet** (it reads the shipped `ladderPOJO.js`, like `--phrases`, so a race can't name a rung the game lacks — ~1 s rebuild); only `--suggest` does, which is why the script's WordNet gate became `NO_WORDNET`. **What the build enforces:** same tree, neither endpoint a root, par 4–6, no duplicates, and — asymmetrically, the load-bearing rule — **every rung on the DOWN leg must be in `2of12.txt`**, because the descent is the half the player *types* while General climbs with no input at all. `--suggest` adds a **concrete-tree allowlist** on top (animal/plant/fruit/food/vehicle/tool/instrument/clothing/container/building/music/…; `person` and `location` deliberately out — roles and regions race badly, and the abstract half of the forest is where `clericalism ⟶ monetarism` comes from). **`racePOJO.js` round-robins the list across trees before writing**, because the daily indexes it positionally so the file's order *is* the calendar (26 of 238 consecutive days share a tree, all in the `animal`-only tail); par is **baked** so the goal display can name it before the 337 KB corpus loads, and `racePOJO.js` therefore loads separately as well as inside `loadRace`. Sense traps kept out of the pool and recorded in the source file's header: `dolphin` (WordNet files it under FISH), `peacock` (under BUTTERFLY), `seal`, `bat`, `drill`, `crane`. One tree is in *because* it surprises — WordNet's `instrument` is "a device that requires skill", so `clock ⟶ flute` and `bullet ⟶ violin` are true and are exactly the lesson. **Still open (§12.7):** the tempting wrong turn — the plant races end at `rose`/`tulip`/`violet`, which are under `bush`/`plant`/`viola` and *not* under `flower`, so typing `flower` at `plant` is a legal step into a branch the target isn't in (a detour, not a dead end). **The rest of M10 remains** (lock, stats, share, give-up, hint chrome, the map's lock/unlock pair). **The next work is doc §12.8's play-test fix list.** §12.8 opened 2026-08-24 with the finding that reframed the mode: the first play read as *the wrong game entirely* purely because **the goal is never communicated** — the player was never told to head for `salmon`, even though fewest-moves-A-to-B is exactly what M9 built. Root cause was layout, not logic: `#race-banner` (which did carry `poodle ⟶ salmon · Par 5`) was **the only in-flow element among out-of-flow siblings** (`#sentence-container` absolute, `#input-container` fixed, `.shine` fixed at top:80px), so it painted under the fixed title and was never seen. **Note 1 BUILT 2026-08-24 — the goal display replaces the edit box, and `#race-banner` is retired** (two places showing race state is one too many): picking Word Race in the dropdown hides `#input-sentence` and paints `#race-goal` in its place — the route `poodle ⟶ salmon`, each word in its hero's colour, over a line carrying the rule — because that box has no job before a run starts and an empty text field reads as *type your sentence here*, the exact wrong instruction; Pow! then brings the box back **beneath** the display as the move box (the note's open question, settled the way it leaned: the destination stays on screen for the whole run — though Note 2 below then narrowed *when* the box comes back) and the second line becomes `Par 5 · 2 moves · …`. Two things worth keeping: the swap fires on the **`change` event** (covering both the native `<select>` and the custom dropdown, which dispatches one) guarded by `raceActive()`, and the full-width display needs `flex-wrap` on the flex-row `#input-container` — switched on by a **`.race-on` class, never unconditionally**, since that row is already a hair wider than its 70% container on a narrowish desktop and wrapping by default would drop the Pow! button to a second line **in every mode**. **Note 2 BUILT 2026-08-24 — the move box appears only when Keen Arrow asks for a word.** The ambiguity was put to the dev and settled: **Keen's shot on the word you're STANDING on is the ask**, so a descent is now two of his shots with the typing between them (shoot `dog` → the box opens, focused, *a kind of dog?* → type `beagle`, Enter → it spawns below → shoot `beagle` → travel, box closes), and the box is never on screen with nothing to type. Chosen over *"the box appears when Keen is the selected hero"* because a box opened by a **shot on a word** says which word it is asking about, in the only vocabulary this game has — and it gives Keen something to do on a field where, until you type, he has no target at all (which is what made the "once a descent has happened" reading circular). The extra shot is free: **moves** are the score, not shots. It does mean Keen's one action means two things in sequence, which §2.5 refused for *free play* — the difference is that free play's two meanings were indistinguishable once drawn, whereas here there is exactly one thing on the field to shoot at. Three things worth keeping: **Keen's span id MOVES** (`aimKeenAt`) rather than sitting on both spans — and it must **start** on the middle word, because `heroToTheRescue` builds the team once from the ids present, so an id that only appears later leaves Keen out of the race entirely (same family as the wrapper-span footgun); **the summon blurs the box**, since the global movement handler ignores keystrokes aimed at an input and a focused box would swallow the `↑` the message just asked for (true in M9 too — the box was focused on Pow! and you had to click away before your first shot); and **`canDescend()` is asked before the box opens** (clank + capstone on a word with nothing under it rather than a box for an impossible answer), consulting the **alt** map as well as the main one, since `tree` reaches `oak` only by an alt edge. §12.2's `type a narrower kind` placeholder is gone with it — it was there to stop the field shifting under the player's aim, which costs nothing to drop because it is the last item of a top-anchored column; `.race-word[hidden]` needs `display:none` spelled out to beat `.race-word`'s `inline-block`. One line replaces it: the status line opens a run naming both directions. **Note 3 BUILT 2026-08-25 — YOU CAN'T SKIP A RUNG, which reverses M9's descendant-jump rule.** M9 accepted any true descendant and travelled straight to it (`beagle` at `dog` = 2 rungs for one shot, the expert lane); at `animal` you could type `salmon` and skip `fish`, which is the rung that *is* the lesson — so a move is now exactly one rung, `classifyGuess` splits the old `GUESS.OK` on `rungs === 1` and a true-but-deep descendant becomes its own rejection **`GUESS.DEEPER`**. Three consequences worth knowing: **par is now a floor, not a target** (nobody can finish under it, so everything above par is detours); the "too deep" message must **concede before it refuses** and may name **how far** but never **what's between** — `hound` is the answer, and printing it is §13.5's fog leak again; and `raceTravel` loses its passed-through-rungs walk, which is now provably always empty (a down-move's landing word has the previous word as its direct parent, and an alt move has no main-map intermediates), so a Word Race move lights exactly the word it lands on and §13's "passed-through rungs on a §12.2 jump" task is closed rather than done. `descentFrom` is unchanged and still reports the true rung count — only what the race *accepts* narrowed — and all five `ladderAlt` probe fixes survive because each is a *direct* alt child. More dev notes expected on the rest of the list.**: the sentence is thrown away and **the player *is* a word**, travelling the hierarchy — a daily **A⟶B route race** (`poodle ⟶ salmon`, par 5) scored in moves, plus a tentative 60-second **Deep Dive** (§12.4). Four measurements against the built `ladderPOJO.js` drove the design: the map is a **forest of 1,002 disjoint trees** (so both endpoints must share a tree — `tulip→oak` has no route), **depth caps at 5** (only 32 trees are that tall, so "go deep" must mean *repeated* short dives, not one long one), **84% of words are leaves** (start pool = the 3,835 with a parent *and* a child), and **branching is too wide to draw** (`person` 805 children, `fish` 221) — which is why the input is **typing, not a menu of children**, reversing the doc's earlier "typing isn't what Punctuators does". Interaction is **type to summon, shoot to travel** (up needs no typing — one parent; down spawns the typed word for Keen Arrow to shoot), the existing sentence box is **repurposed** as the move box, **any true descendant is accepted and jumps every rung it crosses** (`dog`→type `beagle` = 2 rungs for 1 shot — measured: without this, 7 of 10 sensible guesses get rejected) — **REVERSED 2026-08-25 by §12.8 Note 3, below: one rung at a time, and those 7 of 10 now bounce with a concede-then-refuse message** — and the rejections (**wrong direction / not a kind of it / not in my book**) must sound different. Par = the LCA path (distribution peaks at 4–6 over 6.4M familiar pairs); pairs are **generated-then-frozen** + positional like §11.7; no fail, no clock, detours are the share stat; a **decoy field is easy mode**. **The one prerequisite that's data work — MEASURED AND BUILT 2026-08-24:** `ladderAlt`, the answer-checking-only alternate-sense map, because a typed `oak` under `tree` was rejected (`oak → wood → material`) and the rejection is a lie. §12.7 said *measure before committing*, so it was, before any engine code: **98 KB raw / 41 KB gzipped**, shipped as **its own file `ladderAltPOJO.js`** (a departure from §12.5 — free play, the phrases and the map all read the main corpus and none of them want this, so only the race pays), built by **`build-ladders.py --alt`**, which reads the shipped `ladderPOJO.js` like `--phrases` so an alt edge can't name a rung the game lacks. It fixes **5 of the 9** lying rejections (`oak`/`maple`/`birch`@`tree`, `chicken`@`bird`, `tuna`@`fish`). **The filter is the part worth remembering:** unfiltered, the map asserted that **dogs, cows, snakes and plants are kinds of PERSON** — WordNet files "a second view of the same thing" (`oak` the tree c=3 / the wood c=1) together with metaphors and slurs (`dog` c=42 / `frump` c=0), and the discriminator is that the junk pairs a dominant sense with a **zero-evidence** one, so an alt sense must carry **≥20% of the word's best-sense count** (`ALT_MIN_SHARE`); that cut the file 38% with **identical acceptance on all 31 probes**. The both-ends-in-`2of12` prune §12.7 floated was measured and **rejected** (9% savings). **A second, harder rejection class was found doing it (doc §12.2):** `tulip`/`rose` are under no sense of `flower`, `redwood` under no sense of `tree` — **unfixable by any alt data**, harmless now (they land in "not a kind of it") but **M10's pair selection must not put a target one hop from such a hole**. **M9 (the engine) BUILT 2026-08-24** — `ladderRace.js` (LCA par, `descentFrom` = main map + one alt hop, five `GUESS` classes, `decoysFor`, `createRace`), the `wordRace` `<option>`, the three-span field, two collision branches, and `GeneralIzationRace`/`KeenArrowRace` **subclassing** the free-play pair so art/SFX/broadsword come along and only `targetId` differs. **The race splits the heroes across TWO span ids** where free play shares one (§4): a race has two words on the field at once and each hero must reach only its own, which the existing id gate then does for free. **Verified against the doc's own numbers** — all six §12.3 sample pairs compute the par printed there (5·5·6·4·2·2). **Footgun hit and recorded: a wrapper span empties the team** — a `MutationRecord`'s `addedNodes` lists only *directly* inserted nodes, never a added node's descendants, so wrapping the field in one container put only the container in `nodeArr`, making both shootable words invisible to collision (empty team → `player` undefined → `animate()` throws every frame, the same white-screen as the other two footguns); the three spans are top-level siblings and the column lives on `#output.race-mode`. **One fix reaching every mode:** the global movement `keydown` had no target check — harmless only because every other mode hides the input box, but Word Race keeps it as the move box, so it now ignores events from an input/textarea/contenteditable. M10 daily / M11 feel. **Phase 4 — the Tree of Kinds — SPECCED 2026-08-22 (doc §13); M12 BUILT 2026-08-23 across two sessions (`ladderMap.js`, then the fill with M3)**: not a mode but a **progress map** overlaying every ladder mode — the whole hierarchy drawn once as **nested circles** (1,002 trees, ≤5 deep), every word you've ever landed on **lit**, panned and zoomed like a map. Three decisions locked: **skeleton visible / fringe fogged** (the 4,837 *internal* words are always drawn and named — the coastline, 4,809 of them in `2of12.txt`; the 25,708 leaves are anonymous buds with a cluster count `dog +33` until visited, so you always see how much is left but can never read an answer off it), **per-parent shelves** as the completion metric (`dog 7/33` — 89% of shelves hold ≤10 children, median 2, only 65 hold >50, so unlike the uncompletable 30,545-word whole it's a real goal; milestones at 25/50/100% keyed off **fractions** because ~10,700 nodes are outside `2of12.txt` and will never be typed), and **an overlay in `punctuators.html`** filled by free play + Restore the Phrase + Word Race alike, one shared `localStorage` record. The layout is a **pure function of the shipped `ladderPOJO.js`** (**zero new data bytes**, no layout file, computed once on first open and never recomputed: what changes as you play is lit-vs-dark, not position; verified 0 coordinate drift on rebuild). Radius follows `√(subtree size)` so area tracks subtree size, which is what makes **pure pan/zoom** work with no wedge remapping despite `person`'s 1,529-wide ring at depth 2 (an equal-angle layout dies there at 0.24°/node); viewport model lifted from Inklings' Sound Board (`sbView`/`sbClampView`/drag-pan/recentre). **The plan's golden-angle spiral was built, measured and REPLACED** — looseness compounds through 5 levels of nesting, so it filled only 4.65% of the field (forest radius 811, **0.54 px per leaf**, i.e. the zoomed-out heat map was an invisible haze); the shipped pack is **front-chain placement** (the d3-pack algorithm, reimplemented inline — an algorithm, not a dependency) plus Badoiu–Clarkson enclosing circles, giving radius **271 / 41.5% fill / 1.61 px per leaf**, with 0 sibling overlaps and 0 children escaping their parent across all 30,545 nodes. `PAD` (parent-hull air) was tuned to **1.02** because the median shelf holds 2 children, so it binds on half the map. **Three LOD tiers** (<3 px = one dot coloured by the subtree's **lit fraction**, recursion stops · 3–20 px = circle + shelf arc + child dots · >20 px = circle + arc + label + shelf counter) bound draw cost by zoom rather than corpus; the <3 px tier *is* the payoff, and its ~30k dots are **batched by ramp colour** (33 `fill()` calls, not 30,000) so a drag doesn't stutter. **Trap worth remembering: `constructor` and `prototype` are real words in the corpus**, so nothing keyed by word may be a plain object — a `{}` lookup invents a multi-parent edge and corrupts subtree sizes; `ladderMap.js` uses `Map`/`Set` throughout and `ladderFunc.js`/`ladderRace.js` will need the same. **The finding that changed build order (§13.7):** a first-child-only Keen Arrow caps every shelf at 1/33 and the map is structurally unfillable in free play — so **branching hyponyms was promoted out of Deferred (§15)** and BUILT with M3 (see above); Word Race fills shelves for free since typing summons any descendant. The named skeleton is a routing atlas, so **the map is disabled during a daily run** (the lock is BUILT dormant in M14, below) and finishing one opens it and **draws your route on it** (detours and all, against the route you could have taken — moved to §12's M11, since a route needs the racing engine to exist). Storage `punctuators.ladderMap` = `{v:1, seen:[…]}`, 5–54 KB realistically (~335 KB even if every word were visited), safe across corpus rebuilds by construction. **M12 as built** = `ladderMap.js` (forest build, front-chain pack, LOD draw, pan/zoom/hit-test, visited set + storage, and the `ladderMapVisit(word)`/`ladderMapVisitAll(words)` **seam M3 will call**), the 🌳 Tree of Kinds button + overlay in `punctuators.html`, its CSS, and one `initLadderMap()` call in `index.js`; `ladderPOJO.js` is **dynamically `import()`ed on first open** (a deliberate departure from §3.3's static-import decision, which existed only to keep `hasLadders`/`wrapLadders` synchronous — nothing on the map's path is), and the §13.5 fog rule was pulled forward from M13 so no intermediate build ever prints leaf names. `ladderMapVisit()` is now called by `climbLadder()` on every rung landed on — including the one you were standing on, so the word you typed lights the first time you shoot it; `?mapseed=N` still lights a deterministic session-only sample (never persisted) and `?map=1` opens the panel. **M13 (the fill) BUILT 2026-08-23** — the **shelf** (`KLIT`, a word's own children lit, derived beside `LIT` in one post-order pass; `SELFLIT` exists because `LIT` is a *total* and can't be asked about one word) becomes a first-class number on **both** sides: on the map a **gold arc** on every circle from twelve o'clock covering `lit/total` (the arc, not the written `7/33` counter, is the load-bearing display — most of the map sits between 3 and 20 px where a counter doesn't fit but a ring reads), a done shelf taking the ramp's existing gold end at tier 1 so a filled region stays gold from orbit (a bucket index, so the 33-`fill()` batching is untouched), a shelf line in the hover readout and `42 of 4,837 shelves filled` in the panel bar; in play the fan's caption carries `kinds of dog · 7/33 found · +26 more` and **25/50/100% milestones are announced where they're earned** — a gold banner *above* the word (the fan owns the space below, and the shot that fills a shelf usually opens the next one) + a **7th SFX** `_shelfMilestone` that deliberately sits clear of §7's six hero cues (a bell that *climbs*, where the others fall). Two departures from the plan: the milestone fires **in play**, not on the map (the plan's "discover it next time you open the panel" makes it barely a milestone), and it needed a **floor of ≥5 children** (`SHELF_MILESTONE_MIN`) — **measured: 33.6% of shelves hold exactly one child**, so unguarded it congratulates you on a "completed shelf" on a third of all first shots; requiring that one arrival can't already cross 25% (`1/total < 0.25`) *derives* the floor rather than picking it, leaving 1,416 shelves able to announce and each firing at most 3 times ever. `shelfProgress`/`shelfMilestoneCrossed` live in `ladderFunc.js` (taking `seen` as a parameter, same map-independence as `shelfFor`); `noteShelfProgress` hangs off `ladderMapVisit`'s newly-lit return, so re-shooting a lit word is silent. **M14 (the feel) BUILT 2026-08-23 — the map is complete.** Two of its four planned items were **blocked, not deferred by choice**: §13.8's daily-run guard and post-game route overlay both assume a daily, and neither §11 nor §12 has any game code — so M14 split. **Built:** (a) the **ancestry breadcrumb** — the readout carried one hop (`dog — a kind of mammal`), which is the *least* useful hop since the picture already draws `dog` inside `mammal`; it now carries the whole chain broad→narrow (`animal › mammal › dog · lit · 7/33 kinds`), and **it can never need redacting** because every ancestor has a child by definition, so every ancestor is internal and §13.5 names all of them (only the hovered word itself can be fogged). Depth caps at 5 so a path is ≤6 words and needs no truncation — but the longest real one measures **92 chars** (`immorality › … › sanctimoniousness`), so the readout gained a `max-width` and wraps; and since `readout()` fires on every pointermove and now builds HTML, a `readShown` memo (the guard `paintStat()` already used) rebuilds only when the hovered node changes. (b) The **share string** `🌳 Tree of Kinds · 1,204 words · 42 shelves · ANIMAL 31%` behind a 📋 bar button (clipboard + textarea fallback, Critter Hunt's `copyShare` shape); its third term is **not** the most-lit tree — `person` is 4,415 nodes, **14% of the corpus**, so that would print PERSON for nearly every player and a stat that never moves or differs isn't a stat — but the **highest lit fraction among the 88 roots holding ≥50 nodes** (the floor stops one of the 465 sub-5-node shrubs winning at 100%); it's spoiler-free **by construction and verified — 0 of the 1,002 roots are leaves**, since a root appears in `ladderDown` only as a key. (c) The map's **own help card**, shown once on first open and reopenable with `?` (the hero modal is the right place to *mention* the map and the wrong place to explain it — you read it before ever opening the panel); its dismissed flag rides the **same storage record with no version bump**, since `loadSeen()` only ever asks for `rec.seen`. (d) The **daily-run guard BUILT DORMANT** — `ladderMapLock(reason)`/`ladderMapUnlock()`/`isLadderMapLocked()`, **no caller on purpose**: the lock is map-side work, so writing it now means §11/§12 each add one line on daily start and finish (recorded in both wiring tables) instead of learning `ladderMap.js`; a locked 🌳 button dims, takes a 🔒 and **says why on click** (a dead button says nothing), a lock arriving mid-session closes an open map, and `?maplock=<reason>` exercises the whole path until a daily exists. **The post-game route overlay left M14 for §12's M11** — a route is the racing mode's own artifact and there is nothing to draw without the engine. **FOG LEAK found in play and FIXED 2026-08-24 (doc §13.5): SKELETON NOW MEANS TWO OR MORE KINDS**, not merely "internal". A word whose whole branch is ONE child is a leaf with a tail, not coastline, and naming it just printed an answer — `kinsman 0/4` handed over `brother`, `nephew` and `uncle` (one child each), fogging only the true leaf `brethren`, so three of four answers were readable off the map. **1,623 of the 4,837 internal words (33.6%) are single-child parents** — the same third that forced M13's milestone floor, for the same underlying reason. Fixed by collapsing the rule into **one predicate `isNamed(i) = isSkeleton(i) || visited`** used by every place that can print a word (canvas labels, readout, breadcrumb, share string); it had been spelled inline as `KIDS[i] || visited` in two of them, which is how the hole stayed invisible. **Verified: readable names 3,835 → 2,468** (the rest are multi-child parents — §13.2's accepted trade), and `kinsman` is a genuine 0/4. **Only the name hides, never the circle** (containment *is* the layout) and the shelf arc still turns on a fogged word — an arc spells nothing. **It cost M14's breadcrumb its best property**: "can never need redacting" was true only of the old rule, so a fogged ancestor now draws as a dim `•` holding its place — *unless the hovered word's own name is on screen*, since you can then type that word and broaden from it, putting everything above it one General shot away (naming a chain you can already walk isn't a spoiler); 1,829 of 30,545 words have a hidden rung, which reappears the moment you reach the word below it. Also **256 of the 1,002 roots go anonymous**, but all are shrubs (largest 13 nodes; the largest fogged word anywhere is `specie` at 19), and the share string was already safe — **0 of the 88 roots in its ≥50-node pool are single-child** — but `bestTree()` gained an `isNamed()` guard so a corpus rebuild can't break it. Open: the name (`The Tree of Kinds` recommended), whether the 810 shrub trees are drawn, and whether Inklings shares the map; **what a shelf milestone pays is SETTLED — cosmetic, but announced in play** (a light meta-layer is still possible and nothing in M13 forecloses it). Also §14, a parking lot of unbuilt mechanics for the two characters across both games (the Bureaucrat's Draft, Ladder Golf, the Specificity Range in Inklings).](docs/punctuators-ladder.md) |

The repo also holds many undocumented tools/pages (ambigram generators, anagram/palindrome builders, an
IPA game, `typing.html`, etc.). Those have no `docs/` entry yet — read the file before changing it.

## Shared conventions (apply to all of the above)

- **"ship it"** — When the user says "ship it": **first run a consistency pass** (unless the commit is
  trivial/mechanical — a typo, formatting, a rename — where drift is unlikely), then `git add .`, then
  `git commit -m '<concise, accurate message reflecting what actually changed>'`, then `git push`. No
  confirmation needed for the commit itself — just do it. (End commit messages with the `Co-Authored-By`
  line per the harness instructions.)
  - **Consistency pass (before committing):** review this session's changes for stale/contradictory
    **code comments in changed files**, **`docs/*.md`** (files changed this session or that describe the
    changed code — e.g. BUILT/not-built status markers), and **`CLAUDE.md` + the `~/.claude` memory files**.
    **Auto-fix the obvious** stale wording as part of the same commit; **pause and ask** before committing
    only when a fix is ambiguous or a judgment call. (Scope is the changed surface — don't audit unrelated
    unchanged files.)
- **Vanilla, single-file, no build step.** Each game is one self-contained HTML file (inline CSS + JS),
  no frameworks, no bundler. Don't add dependencies unless a feature truly requires it (say so and why).
  Intentional exceptions are documented per-game (e.g. Inklings fetches a bundled local WordNet dictionary
  (`data/dictionary.json` + `data/inflections.json`), the Nouns-wing shelf index (`data/noun-books.json`),
  the verb-category map for stat ladders (`data/verb-cats.json`), the adjective→potion dumbbell map
  (`data/adj-attrs.json`, built by `build_adj_attrs.py`), room layouts (`data/rooms/*.json`), the heraldry
  blazon roster for the DEV-only Blazon Shield (`data/blazon.json`), the WordNet relation graph that powers
  the Curator's per-word relatives page (`data/wordnet-relations.json`, lazy-loaded on first curator open),
  the hand-written scenario frames + swappable noun list for the Excla Machine daily interjection puzzle
  (`data/excla-scenarios.json`), the World Atlas layered place raster (`data/atlas-world.json`, built by
  `build_geo.py` from public-domain Natural Earth — countries/capitals + seas, lakes and mountain peaks,
  475 spellable names; lazy-load on first Atlas open),
  and `2of12.txt` — all local project files, no runtime third-party API).
- **Ask clarifying questions before non-trivial work.** For any meaningful change, surface the genuine
  design forks and get answers before writing code. Prefer a recommendation over an exhaustive survey.
- **Parse-check, don't build a test harness.** These are browser games with no test rig; sanity-check
  that the inline `<script>` parses and let the user verify behavior in the browser. Don't run headless
  node/vm harnesses for game logic.
- **Don't reorganize the file layout.** The site is served flat and pages are interlinked; game HTML
  files reference **shared assets by relative path**, so moving them breaks URLs, navigation, and asset
  loading. Notable shared assets: `Alpha.png` (Spin Nids + Inklings glyph sheet), `2of12.txt` (punctuators
  + Inklings), `Lumberjack_Jack.png` (Inklings player). Keep game files and assets where they are.

## Notes

- The git remote reports it moved to `github.com/exclaMachine/PunctuatorsGame.git` (origin still points at
  the old `CanvasPunctuatorsGame` URL; pushes redirect fine).
- A persistent memory store at `~/.claude/projects/.../memory/` holds cross-session feedback/preferences;
  its `MEMORY.md` index is loaded each session.
- **Punctuators ambigram wordplay (Ambigrambador).** The `ambigramPOJO` in `AmbigramFunc.js` (`word →
  180°-partner`) is **generated offline** by `build-ambigram-pojo.js` from `enable1.txt` using Spin Nids'
  `AM` 180° map — a word counts iff every letter maps and the reversed+mapped result is itself a real word
  (636 dictionary-validated pairs, partners lowercased for a normal-font reveal; re-run the script to
  regenerate). Detection wraps matches as `<span data-ambigram="…">` and the hit-time reveal is a two-face
  180° spin (`.ambi-spin` in `index.css`, ported from Spin Nids' `chip-rot`) landing on the partner word.
  Each wordplay mode with no matches in the sentence notifies the player instead of running (shared guard
  pattern: `hasAmbigrams`/`hasHomophones`/`hasAnagrams`). **Only 180° rotational is ported so far**; Spin
  Nids' 90°/270° rotations and horizontal/vertical mirrors (`MH`/`MV` maps) are a possible follow-up.
- **Real instrument samples (started 2026-07-28).** `sounds/instruments/<name>/<Note>.mp3` holds real-recording
  mp3s (FluidR3_GM via gleitz/midi-js-soundfonts; `sounds/CREDITS.md`; refetch/add with
  `fetch-instrument-samples.sh` — **black keys are flats**, source folder `<name>-mp3`). Approach chosen =
  **anchor + pitch-shift**: 13 samples/instrument (one every 3 semitones, C3–C6), play nearest + `playbackRate`
  shift ≤1.5 st (indistinguishable from per-note, ⅓ the files). **BUILT into Critter Hunt** — 32 pitched
  instruments via `playInstrument`/`playSample`; Drum/Conga stay synth. `instrument-sound-lab.html` = a
  standalone preview (of the original 7) at any pitch. **BUILT into Mujicians 2026-07-28** — a **sampled-voice branch** in `playVoice` (`type:"sample"` VOICES +
  `renderSample`/`renderSynth` split, `buildDeck` preload) makes the real instruments **pitched cards** (Free
  Play + Sound Collective). **Set grown 7→20 on 2026-07-29** with an obscure/world batch (shamisen, koto,
  shakuhachi, sitar, shehnai, bagpipes, kalimba, marimba, glockenspiel, celesta, harpsichord, dulcimer, harp),
  then **20→32 on 2026-08-02** with an orchestral/world/chromatic-percussion batch (piano, cello, trombone, tuba,
  clarinet, oboe, pan flute, accordion, xylophone, steel drums, timpani, taiko) — all **wired into BOTH games**
  (Mujicians VOICES/INSTRUMENTS + Critter Hunt's pool + `CRITTER_SAMPLE_VOICE` boss-reward map). Loads via
  fetch/decodeAudioData → needs http serving, not `file://`.
- **Real animal samples (2026-07-28; grown to 20 species 2026-08-01; 26 on 2026-08-05).** A parallel set for **Critter Hunt**: `sounds/animals/<slug>.mp3` holds
  **26 hand-curated Wikimedia-Commons animal cries**, each a recording OF a specific species so the name matches the
  call (Red Fox, Chestnut-fronted Macaw, Rattlesnake, Great Horned Owl, Hoary Bat, Field Cricket, Banded Bullfrog,
  African Elephant, American Alligator, Orca, Gray Wolf, Humpback Whale, Mallard, Indian Peafowl, Howler Monkey, Wild
  Turkey, Goat, Rooster, Cow, Sheep, Horse, Common Loon, Tokay Gecko, Honey Bee, Domestic Cat, African Penguin;
  CC0 / Public-domain / CC-BY / CC-BY-SA, **none ND** → repitch-safe; `fetch-animal-samples.sh` downloads+trims+normalises
  via **ffmpeg** (now with a descriptive User-Agent — Wikimedia 429s default curl UAs), provenance in `data/critter-credits.json` + `sounds/CREDITS.md`). Unlike the instruments these are
  **one one-shot per animal** — played by `playAnimalSound` (full clip) *and*, cropped+pitched, by `playAnimalTuned`;
  animals are ▶-playable on their cards + in the won collection. **Animal → Mujicians voice BUILT 2026-08-01:** rather
  than a dev deciding pitched-vs-percussion per animal, the **player tunes a won cry**. **REWORKED 2026-08-02:** the old
  inline win-flow dialog (`showAnimalTuner`) was **removed** — keeping an animal now claims it with a forgiving default
  (`DEFAULT_CFG`: role guess + a wide **1.5 s** window from `off:0` so nothing is silent out of the box), and tuning
  moved into **`animal-sound-lab.html` as the shared, re-openable editor** (role toggle + note-length/anchor/start-offset
  sliders + keyboard/scale/drum audition + per-animal **Save**; lists **only collected animals** — `?all=1` = full dev
  roster). The lab's Save writes the **shared store `localStorage["critterhunt.animalVoices"]`** ({slug:{nm,em,role,anchor,off,len}}),
  opened from Critter Hunt's win modal + a collection **🎚 Tune sounds** button + a Mujicians **Home 🐾 Tune Sounds**
  button. Mujicians' `registerAnimalVoices()` (run at load) reads that store → builds a `type:"animal"` VOICE
  (`renderAnimal`: single-file `sounds/animals/<slug>.mp3`, cropped+pitched — pitched repitches the card's note from
  anchor & holds the note's length; one-shot = fixed pitch + fixed `len`) + a Free-Play `INSTRUMENTS` card + a
  Sound-Collective entry per animal; a boss-return animal reward grants the same. **Deferred:** routing one-shot animals
  through the true `{drum}`/`DRUM_VOICES` lane (they play as fixed-pitch VOICES for now); campaign decks stay curated
  (animals are Free-Play only). Same http-serving requirement.
- **Phonetic palindromes / semordnilaps (for the IPA fan game).** `build-phonetic-wordplay.js` (repo root,
  `node build-phonetic-wordplay.js` to regenerate) scans **enable1.txt** words × **text-to-ipa** pronunciations
  (`text-to-ipa-master/lib/ipadict.txt`, CMU→IPA), tokenises by phoneme (diphthongs/affricates atomic), and
  converts symbols to **IPA-fan-game/ipa_words.js convention** (ə ɝ tʃ dʒ eɪ oʊ aɪ aʊ ɔɪ ɡ). Emits three files:
  `data/ipa-palindromes.json` (sound reads same reversed — 94), `data/ipa-semordnilaps-exact.json` (phoneme-exact
  reversal pairs incl. `-s` reversals — 658), `data/ipa-semordnilaps-loose.json` (loose-only pairs after merging
  near-equiv vowels {i,ɪ}{u,ʊ}{ə,ɝ}{ɑ,ɔ}, disjoint from exact — 371). **Data only; not yet wired into any game.**
